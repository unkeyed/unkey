# GitHub App credentials for webhook-triggered deployments
# Copy this to .env.github and fill in your values

# GitHub App ID (from your GitHub App settings)
UNKEY_GITHUB_APP_ID=2721195

# Webhook secret (must match what you set in GitHub App settings)
UNKEY_GITHUB_APP_WEBHOOK_SECRET=supersecret

# GitHub App name (slug) - used by dashboard to generate install URLs
NEXT_PUBLIC_GITHUB_APP_NAME="unkey-staging"

# IMPORTANT: Put your GitHub App private key in a separate file:
# Save your .pem file as: dev/.github-private-key.pem
