# Download your private key from GitHub App settings > Private keys
# Save it as .github-private-key.pem (without .example)
# The PEM file begins with the standard RSA private key header and ends with its matching footer.
