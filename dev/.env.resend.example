# Resend API key for the Deploy spend-cap budget alert emails (optional).
# Copy this to .env.resend to actually send the alerts. Without it the worker
# uses a noop sender that only logs; enforcement (suspend/resume) is unaffected.
RESEND_API_KEY=re_...
