// Package metrics defines Prometheus metrics used by pkg/mysql wrappers.
//
// The package is intentionally narrow in scope and only exposes collectors
// required by the shared MySQL abstraction.
package metrics
