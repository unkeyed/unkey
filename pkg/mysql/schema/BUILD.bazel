# Per-table SQL schema files generated by split-schema from drizzle-kit output.
# Used by //pkg/dockertest to bootstrap test MySQL containers and by sqlc configs
# to generate type-safe query code.
filegroup(
    name = "schema",
    srcs = glob(["*.sql"]),
    visibility = ["//visibility:public"],
)
