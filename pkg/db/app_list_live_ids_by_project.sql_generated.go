// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: app_list_live_ids_by_project.sql

package db

import (
	"context"
)

const listLiveAppIdsByProject = `-- name: ListLiveAppIdsByProject :many
SELECT id
FROM apps
WHERE project_id = ?
  AND deletion_id IS NULL
`

// Returns the IDs of apps under a project that are not already
// scheduled for permanent deletion. Used by the soft-delete cascade
// so a parent's SoftDelete does not disturb apps that were deleted
// independently (those already have their own deletions row with a
// different T).
//
//	SELECT id
//	FROM apps
//	WHERE project_id = ?
//	  AND deletion_id IS NULL
func (q *Queries) ListLiveAppIdsByProject(ctx context.Context, db DBTX, projectID string) ([]string, error) {
	rows, err := db.QueryContext(ctx, listLiveAppIdsByProject, projectID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []string
	for rows.Next() {
		var id string
		if err := rows.Scan(&id); err != nil {
			return nil, err
		}
		items = append(items, id)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
