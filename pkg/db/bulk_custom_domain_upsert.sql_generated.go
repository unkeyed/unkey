// Code generated by sqlc bulk insert plugin. DO NOT EDIT.

package db

import (
	"context"
	"fmt"
	"strings"
)

// bulkUpsertCustomDomain is the base query for bulk insert
const bulkUpsertCustomDomain = `INSERT INTO custom_domains ( id, workspace_id, project_id, environment_id, domain, challenge_type, verification_status, target_cname, created_at ) VALUES %s ON DUPLICATE KEY UPDATE
    workspace_id = VALUES(workspace_id),
    project_id = VALUES(project_id),
    environment_id = VALUES(environment_id),
    challenge_type = VALUES(challenge_type),
    verification_status = VALUES(verification_status),
    target_cname = VALUES(target_cname),
    updated_at = ?`

// UpsertCustomDomain performs bulk insert in a single query
func (q *BulkQueries) UpsertCustomDomain(ctx context.Context, db DBTX, args []UpsertCustomDomainParams) error {

	if len(args) == 0 {
		return nil
	}

	// Build the bulk insert query
	valueClauses := make([]string, len(args))
	for i := range args {
		valueClauses[i] = "(?, ?, ?, ?, ?, ?, ?, ?, ?)"
	}

	bulkQuery := fmt.Sprintf(bulkUpsertCustomDomain, strings.Join(valueClauses, ", "))

	// Collect all arguments
	var allArgs []any
	for _, arg := range args {
		allArgs = append(allArgs, arg.ID)
		allArgs = append(allArgs, arg.WorkspaceID)
		allArgs = append(allArgs, arg.ProjectID)
		allArgs = append(allArgs, arg.EnvironmentID)
		allArgs = append(allArgs, arg.Domain)
		allArgs = append(allArgs, arg.ChallengeType)
		allArgs = append(allArgs, arg.VerificationStatus)
		allArgs = append(allArgs, arg.TargetCname)
		allArgs = append(allArgs, arg.CreatedAt)
	}

	// Add ON DUPLICATE KEY UPDATE parameters (only once, not per row)
	if len(args) > 0 {
		allArgs = append(allArgs, args[0].UpdatedAt)
	}

	// Execute the bulk insert
    _, err := db.ExecContext(ctx, bulkQuery, allArgs...)
    return err
}
