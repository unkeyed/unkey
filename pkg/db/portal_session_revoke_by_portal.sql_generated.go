// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: portal_session_revoke_by_portal.sql

package db

import (
	"context"
	"database/sql"
)

const revokePortalSessionsByPortal = `-- name: RevokePortalSessionsByPortal :execrows
UPDATE portal_sessions
SET revoked_at = ?
WHERE portal_id = ?
  AND workspace_id = ?
  AND revoked_at IS NULL
`

type RevokePortalSessionsByPortalParams struct {
	RevokedAt   sql.NullInt64 `db:"revoked_at"`
	PortalID    string        `db:"portal_id"`
	WorkspaceID string        `db:"workspace_id"`
}

// Revokes every live session belonging to a portal, scoped to the workspace.
//
// A session's keyspace scope is frozen in `scopes` at mint time and the session
// resolver never reads `portals`, so deleting a portal or re-pointing its
// association would otherwise leave end users authenticated against stale scope
// until their access token expired. Returns the row count so the caller can
// record how many sessions were revoked.
//
//	UPDATE portal_sessions
//	SET revoked_at = ?
//	WHERE portal_id = ?
//	  AND workspace_id = ?
//	  AND revoked_at IS NULL
func (q *Queries) RevokePortalSessionsByPortal(ctx context.Context, db DBTX, arg RevokePortalSessionsByPortalParams) (int64, error) {
	result, err := db.ExecContext(ctx, revokePortalSessionsByPortal, arg.RevokedAt, arg.PortalID, arg.WorkspaceID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected()
}
