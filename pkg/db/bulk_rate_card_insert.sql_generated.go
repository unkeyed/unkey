// Code generated by sqlc bulk insert plugin. DO NOT EDIT.

package db

import (
	"context"
	"fmt"
	"strings"
)

// bulkInsertRateCard is the base query for bulk insert
const bulkInsertRateCard = `INSERT INTO rate_cards ( id, workspace_id, name, currency, config, selectable, archived, created_at ) VALUES %s`

// InsertRateCards performs bulk insert in a single query
func (q *BulkQueries) InsertRateCards(ctx context.Context, db DBTX, args []InsertRateCardParams) error {

	if len(args) == 0 {
		return nil
	}

	// Build the bulk insert query
	valueClauses := make([]string, len(args))
	for i := range args {
		valueClauses[i] = "( ?, ?, ?, ?, ?, ?, false, ? )"
	}

	bulkQuery := fmt.Sprintf(bulkInsertRateCard, strings.Join(valueClauses, ", "))

	// Collect all arguments
	var allArgs []any
	for _, arg := range args {
		allArgs = append(allArgs, arg.ID)
		allArgs = append(allArgs, arg.WorkspaceID)
		allArgs = append(allArgs, arg.Name)
		allArgs = append(allArgs, arg.Currency)
		allArgs = append(allArgs, arg.Config)
		allArgs = append(allArgs, arg.Selectable)
		allArgs = append(allArgs, arg.CreatedAt)
	}

	// Execute the bulk insert
	_, err := db.ExecContext(ctx, bulkQuery, allArgs...)
	return err
}
