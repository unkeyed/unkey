// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: portal_find_by_app.sql

package db

import (
	"context"
	"database/sql"
)

const findPortalByApp = `-- name: FindPortalByApp :one
SELECT pk, id, workspace_id, slug, app_id, key_auth_id, enabled, logo_url, primary_color, created_at, updated_at FROM portals
WHERE app_id = ?
  AND workspace_id = ?
LIMIT 1
`

type FindPortalByAppParams struct {
	AppID       sql.NullString `db:"app_id"`
	WorkspaceID string         `db:"workspace_id"`
}

// Resolves the portal mapped to an app within a workspace. Callers that reach a
// portal through its app (rather than through an id or slug) use this.
//
// Workspace-scoped on purpose: `idx_app_id` is unique across the whole table, so
// an unscoped lookup would return another workspace's portal.
//
//	SELECT pk, id, workspace_id, slug, app_id, key_auth_id, enabled, logo_url, primary_color, created_at, updated_at FROM portals
//	WHERE app_id = ?
//	  AND workspace_id = ?
//	LIMIT 1
func (q *Queries) FindPortalByApp(ctx context.Context, db DBTX, arg FindPortalByAppParams) (Portal, error) {
	row := db.QueryRowContext(ctx, findPortalByApp, arg.AppID, arg.WorkspaceID)
	var i Portal
	err := row.Scan(
		&i.Pk,
		&i.ID,
		&i.WorkspaceID,
		&i.Slug,
		&i.AppID,
		&i.KeyAuthID,
		&i.Enabled,
		&i.LogoUrl,
		&i.PrimaryColor,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
