// Code generated by sqlc bulk insert plugin. DO NOT EDIT.

package db

import (
	"context"
	"fmt"
	"strings"
)

// bulkInsertDeletion is the base query for bulk insert
const bulkInsertDeletion = `INSERT INTO ` + "`" + `deletions` + "`" + ` (id, workspace_id, resource_type, resource_id, delete_permanently_at) VALUES %s ON DUPLICATE KEY UPDATE resource_id = resource_id`

// InsertDeletions performs bulk insert in a single query
func (q *BulkQueries) InsertDeletions(ctx context.Context, db DBTX, args []InsertDeletionParams) error {

	if len(args) == 0 {
		return nil
	}

	// Build the bulk insert query
	valueClauses := make([]string, len(args))
	for i := range args {
		valueClauses[i] = "(?, ?, ?, ?, ?)"
	}

	bulkQuery := fmt.Sprintf(bulkInsertDeletion, strings.Join(valueClauses, ", "))

	// Collect all arguments
	var allArgs []any
	for _, arg := range args {
		allArgs = append(allArgs, arg.ID)
		allArgs = append(allArgs, arg.WorkspaceID)
		allArgs = append(allArgs, arg.ResourceType)
		allArgs = append(allArgs, arg.ResourceID)
		allArgs = append(allArgs, arg.DeletePermanentlyAt)
	}

	// Execute the bulk insert
	_, err := db.ExecContext(ctx, bulkQuery, allArgs...)
	return err
}
