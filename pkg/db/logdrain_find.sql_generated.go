// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: logdrain_find.sql

package db

import (
	"context"
)

const findLogdrain = `-- name: FindLogdrain :one
SELECT pk, id, workspace_id, name, stream, config, status, consecutive_failures,
  committed_offset_inserted_at, committed_offset_event_id, next_attempt_at,
  lease_id, fencing_token, lease_expires_at, created_at, updated_at
FROM logdrains WHERE workspace_id = ? AND id = ?
`

type FindLogdrainParams struct {
	WorkspaceID string `db:"workspace_id"`
	ID          string `db:"id"`
}

// Scope reads to the authorized workspace so foreign IDs are indistinguishable
// from missing drains. Credentials stay in the stored protobuf.
//
//	SELECT pk, id, workspace_id, name, stream, config, status, consecutive_failures,
//	  committed_offset_inserted_at, committed_offset_event_id, next_attempt_at,
//	  lease_id, fencing_token, lease_expires_at, created_at, updated_at
//	FROM logdrains WHERE workspace_id = ? AND id = ?
func (q *Queries) FindLogdrain(ctx context.Context, db DBTX, arg FindLogdrainParams) (Logdrain, error) {
	row := db.QueryRowContext(ctx, findLogdrain, arg.WorkspaceID, arg.ID)
	var i Logdrain
	err := row.Scan(
		&i.Pk,
		&i.ID,
		&i.WorkspaceID,
		&i.Name,
		&i.Stream,
		&i.Config,
		&i.Status,
		&i.ConsecutiveFailures,
		&i.CommittedOffsetInsertedAt,
		&i.CommittedOffsetEventID,
		&i.NextAttemptAt,
		&i.LeaseID,
		&i.FencingToken,
		&i.LeaseExpiresAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
