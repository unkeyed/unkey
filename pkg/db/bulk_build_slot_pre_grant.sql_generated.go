// Code generated by sqlc bulk insert plugin. DO NOT EDIT.

package db

import (
	"context"
	"fmt"
	"strings"
)

// bulkPreGrantBuildSlot is the base query for bulk insert
const bulkPreGrantBuildSlot = `INSERT INTO build_slots (deployment_id, workspace_id, acquired_at) SELECT ?, ?, ? WHERE EXISTS ( SELECT 1 FROM deployments d WHERE d.id = ? AND d.status NOT IN (/*SLICE:terminal_statuses*/?) ) VALUES %s`

// PreGrantBuildSlot performs bulk insert in a single query
func (q *BulkQueries) PreGrantBuildSlot(ctx context.Context, db DBTX, args []PreGrantBuildSlotParams) error {

	if len(args) == 0 {
		return nil
	}

	// Build the bulk insert query
	valueClauses := make([]string, len(args))
	for i := range args {
		valueClauses[i] = "()"
	}

	bulkQuery := fmt.Sprintf(bulkPreGrantBuildSlot, strings.Join(valueClauses, ", "))

	// Collect all arguments
	var allArgs []any
	for _, arg := range args {
		allArgs = append(allArgs, arg.DeploymentID)
		allArgs = append(allArgs, arg.WorkspaceID)
		allArgs = append(allArgs, arg.AcquiredAt)
		allArgs = append(allArgs, arg.DeploymentID)
		allArgs = append(allArgs, arg.TerminalStatuses)
	}

	// Execute the bulk insert
	_, err := db.ExecContext(ctx, bulkQuery, allArgs...)
	return err
}
