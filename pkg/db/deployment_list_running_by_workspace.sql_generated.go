// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: deployment_list_running_by_workspace.sql

package db

import (
	"context"
	"database/sql"
)

const listRunningDeploymentsByWorkspaceId = `-- name: ListRunningDeploymentsByWorkspaceId :many
SELECT
  d.id,
  d.app_id,
  a.current_deployment_id
FROM deployments d
JOIN apps a ON a.id = d.app_id
WHERE d.workspace_id = ?
  AND d.desired_state = 'running'
  AND d.status NOT IN ('stopped', 'failed', 'cancelled', 'superseded', 'skipped')
`

type ListRunningDeploymentsByWorkspaceIdRow struct {
	ID                  string         `db:"id"`
	AppID               string         `db:"app_id"`
	CurrentDeploymentID sql.NullString `db:"current_deployment_id"`
}

// Running deployments for a workspace that still have (or will soon have) live
// compute: desired_state 'running' and not already drained/terminal. Joins apps
// so the caller knows, per deployment, whether it is its app's current
// deployment and therefore must have current_deployment_id cleared before its
// desired state can change.
//
//	SELECT
//	  d.id,
//	  d.app_id,
//	  a.current_deployment_id
//	FROM deployments d
//	JOIN apps a ON a.id = d.app_id
//	WHERE d.workspace_id = ?
//	  AND d.desired_state = 'running'
//	  AND d.status NOT IN ('stopped', 'failed', 'cancelled', 'superseded', 'skipped')
func (q *Queries) ListRunningDeploymentsByWorkspaceId(ctx context.Context, db DBTX, workspaceID string) ([]ListRunningDeploymentsByWorkspaceIdRow, error) {
	rows, err := db.QueryContext(ctx, listRunningDeploymentsByWorkspaceId, workspaceID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListRunningDeploymentsByWorkspaceIdRow
	for rows.Next() {
		var i ListRunningDeploymentsByWorkspaceIdRow
		if err := rows.Scan(&i.ID, &i.AppID, &i.CurrentDeploymentID); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
