// Code generated by sqlc bulk insert plugin. DO NOT EDIT.

package db

import (
	"context"
	"fmt"
	"strings"
)

// bulkRecordLogDrainFailure is the base query for bulk insert
const bulkRecordLogDrainFailure = `INSERT INTO log_drain_state ( drain_id, last_delivery_at, last_attempt_at, last_error, consecutive_failures, paused_reason, total_records_delivered, updated_at ) VALUES %s ON DUPLICATE KEY UPDATE
    last_attempt_at = VALUES(last_attempt_at),
    last_error = VALUES(last_error),
    consecutive_failures = log_drain_state.consecutive_failures + 1,
    paused_reason = VALUES(paused_reason),
    updated_at = VALUES(updated_at)`

// RecordLogDrainFailure performs bulk insert in a single query
func (q *BulkQueries) RecordLogDrainFailure(ctx context.Context, db DBTX, args []RecordLogDrainFailureParams) error {

	if len(args) == 0 {
		return nil
	}

	// Build the bulk insert query
	valueClauses := make([]string, len(args))
	for i := range args {
		valueClauses[i] = "(?, NULL, ?, ?, 1, ?, 0, ?)"
	}

	bulkQuery := fmt.Sprintf(bulkRecordLogDrainFailure, strings.Join(valueClauses, ", "))

	// Collect all arguments
	var allArgs []any
	for _, arg := range args {
		allArgs = append(allArgs, arg.DrainID)
		allArgs = append(allArgs, arg.LastAttemptAt)
		allArgs = append(allArgs, arg.LastError)
		allArgs = append(allArgs, arg.PausedReason)
		allArgs = append(allArgs, arg.UpdatedAt)
	}

	// Execute the bulk insert
	_, err := db.ExecContext(ctx, bulkQuery, allArgs...)
	return err
}
