// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: portal_find_id_by_app_any_workspace.sql

package db

import (
	"context"
	"database/sql"
)

const findPortalIdByAppAnyWorkspace = `-- name: FindPortalIdByAppAnyWorkspace :one
SELECT id FROM portals
WHERE app_id = ?
LIMIT 1
`

// Reports whether any portal already claims this app, across every workspace.
//
// Deliberately unscoped, unlike portal_find_by_app.sql: `idx_app_id` is unique
// table-wide, so a create or update can collide with a portal the caller cannot
// see. This answers "is the association free" for the conflict pre-check. It
// returns only the id -- never the owning workspace -- so the caller cannot use
// a conflict to probe another tenant.
//
//	SELECT id FROM portals
//	WHERE app_id = ?
//	LIMIT 1
func (q *Queries) FindPortalIdByAppAnyWorkspace(ctx context.Context, db DBTX, appID sql.NullString) (string, error) {
	row := db.QueryRowContext(ctx, findPortalIdByAppAnyWorkspace, appID)
	var id string
	err := row.Scan(&id)
	return id, err
}
