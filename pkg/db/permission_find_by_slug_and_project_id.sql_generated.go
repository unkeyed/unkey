// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: permission_find_by_slug_and_project_id.sql

package db

import (
	"context"
)

const findPermissionBySlugAndProjectID = `-- name: FindPermissionBySlugAndProjectID :one
SELECT permissions.pk, permissions.id, permissions.workspace_id, permissions.project_id, permissions.name, permissions.slug, permissions.description, permissions.created_at_m, permissions.updated_at_m
FROM permissions
WHERE slug = ?
AND project_id = ?
AND workspace_id = ?
LIMIT 1
`

type FindPermissionBySlugAndProjectIDParams struct {
	Slug        string `db:"slug"`
	ProjectID   string `db:"project_id"`
	WorkspaceID string `db:"workspace_id"`
}

// FindPermissionBySlugAndProjectID resolves a slug in one project while the
// workspace predicate preserves tenant isolation.
//
//	SELECT permissions.pk, permissions.id, permissions.workspace_id, permissions.project_id, permissions.name, permissions.slug, permissions.description, permissions.created_at_m, permissions.updated_at_m
//	FROM permissions
//	WHERE slug = ?
//	AND project_id = ?
//	AND workspace_id = ?
//	LIMIT 1
func (q *Queries) FindPermissionBySlugAndProjectID(ctx context.Context, db DBTX, arg FindPermissionBySlugAndProjectIDParams) (Permission, error) {
	row := db.QueryRowContext(ctx, findPermissionBySlugAndProjectID, arg.Slug, arg.ProjectID, arg.WorkspaceID)
	var i Permission
	err := row.Scan(
		&i.Pk,
		&i.ID,
		&i.WorkspaceID,
		&i.ProjectID,
		&i.Name,
		&i.Slug,
		&i.Description,
		&i.CreatedAtM,
		&i.UpdatedAtM,
	)
	return i, err
}
