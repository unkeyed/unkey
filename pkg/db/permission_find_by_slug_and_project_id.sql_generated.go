// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: permission_find_by_slug_and_project_id.sql

package db

import (
	"context"
)

const findPermissionBySlugAndProjectID = `-- name: FindPermissionBySlugAndProjectID :one
SELECT permissions.pk, permissions.id, permissions.workspace_id, permissions.project_id, permissions.name, permissions.slug, permissions.description, permissions.created_at_m, permissions.updated_at_m
FROM permissions
WHERE slug = ?
  AND workspace_id = ?
  AND project_id = ?
LIMIT 1
`

type FindPermissionBySlugAndProjectIDParams struct {
	Slug        string `db:"slug"`
	WorkspaceID string `db:"workspace_id"`
	ProjectID   string `db:"project_id"`
}

// FindPermissionBySlugAndProjectID resolves a duplicate insert to the existing
// row in the requested project. The workspace filter preserves tenant isolation.
//
//	SELECT permissions.pk, permissions.id, permissions.workspace_id, permissions.project_id, permissions.name, permissions.slug, permissions.description, permissions.created_at_m, permissions.updated_at_m
//	FROM permissions
//	WHERE slug = ?
//	  AND workspace_id = ?
//	  AND project_id = ?
//	LIMIT 1
func (q *Queries) FindPermissionBySlugAndProjectID(ctx context.Context, db DBTX, arg FindPermissionBySlugAndProjectIDParams) (Permission, error) {
	row := db.QueryRowContext(ctx, findPermissionBySlugAndProjectID, arg.Slug, arg.WorkspaceID, arg.ProjectID)
	var i Permission
	err := row.Scan(
		&i.Pk,
		&i.ID,
		&i.WorkspaceID,
		&i.ProjectID,
		&i.Name,
		&i.Slug,
		&i.Description,
		&i.CreatedAtM,
		&i.UpdatedAtM,
	)
	return i, err
}
