// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: environment_list_live_ids_by_app.sql

package db

import (
	"context"
)

const listLiveEnvironmentIdsByApp = `-- name: ListLiveEnvironmentIdsByApp :many
SELECT id
FROM environments
WHERE app_id = ?
  AND deletion_id IS NULL
`

// Returns the IDs of environments under an app that are not already
// scheduled for permanent deletion. Same rationale as the live-apps
// variant: cascaded soft-delete skips independently-deleted children.
//
//	SELECT id
//	FROM environments
//	WHERE app_id = ?
//	  AND deletion_id IS NULL
func (q *Queries) ListLiveEnvironmentIdsByApp(ctx context.Context, db DBTX, appID string) ([]string, error) {
	rows, err := db.QueryContext(ctx, listLiveEnvironmentIdsByApp, appID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []string
	for rows.Next() {
		var id string
		if err := rows.Scan(&id); err != nil {
			return nil, err
		}
		items = append(items, id)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
