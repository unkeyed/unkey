// Code generated by sqlc bulk insert plugin. DO NOT EDIT.

package db

import (
	"context"
	"fmt"
	"strings"
)

// bulkRecordLogDrainSuccess is the base query for bulk insert
const bulkRecordLogDrainSuccess = `INSERT INTO log_drain_state ( drain_id, last_delivery_at, last_attempt_at, last_error, consecutive_failures, paused_reason, total_records_delivered, updated_at ) VALUES %s ON DUPLICATE KEY UPDATE
    last_delivery_at = VALUES(last_delivery_at),
    last_attempt_at = VALUES(last_attempt_at),
    last_error = NULL,
    consecutive_failures = 0,
    paused_reason = NULL,
    total_records_delivered = log_drain_state.total_records_delivered + VALUES(total_records_delivered),
    updated_at = VALUES(updated_at)`

// RecordLogDrainSuccess performs bulk insert in a single query
func (q *BulkQueries) RecordLogDrainSuccess(ctx context.Context, db DBTX, args []RecordLogDrainSuccessParams) error {

	if len(args) == 0 {
		return nil
	}

	// Build the bulk insert query
	valueClauses := make([]string, len(args))
	for i := range args {
		valueClauses[i] = "(?, ?, ?, NULL, 0, NULL, ?, ?)"
	}

	bulkQuery := fmt.Sprintf(bulkRecordLogDrainSuccess, strings.Join(valueClauses, ", "))

	// Collect all arguments
	var allArgs []any
	for _, arg := range args {
		allArgs = append(allArgs, arg.DrainID)
		allArgs = append(allArgs, arg.LastDeliveryAt)
		allArgs = append(allArgs, arg.LastAttemptAt)
		allArgs = append(allArgs, arg.TotalRecordsDelivered)
		allArgs = append(allArgs, arg.UpdatedAt)
	}

	// Execute the bulk insert
	_, err := db.ExecContext(ctx, bulkQuery, allArgs...)
	return err
}
