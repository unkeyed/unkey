// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: app_build_settings_update.sql

package db

import (
	"context"
	"database/sql"

	dbtype "github.com/unkeyed/unkey/pkg/db/types"
)

const updateAppBuildSettings = `-- name: UpdateAppBuildSettings :exec
UPDATE app_build_settings t
SET
    dockerfile = CASE
        WHEN CAST(? AS UNSIGNED) = 1 THEN ?
        ELSE t.dockerfile
    END,
    docker_context = CASE
        WHEN CAST(? AS UNSIGNED) = 1 THEN ?
        ELSE t.docker_context
    END,
    watch_paths = CASE
        WHEN CAST(? AS UNSIGNED) = 1 THEN ?
        ELSE t.watch_paths
    END,
    auto_deploy = CASE
        WHEN CAST(? AS UNSIGNED) = 1 THEN ?
        ELSE t.auto_deploy
    END,
    updated_at = ?
WHERE workspace_id = ?
  AND app_id = ?
  AND environment_id = ?
`

type UpdateAppBuildSettingsParams struct {
	DockerfileSpecified    int64              `db:"dockerfile_specified"`
	Dockerfile             sql.NullString     `db:"dockerfile"`
	DockerContextSpecified int64              `db:"docker_context_specified"`
	DockerContext          string             `db:"docker_context"`
	WatchPathsSpecified    int64              `db:"watch_paths_specified"`
	WatchPaths             dbtype.StringSlice `db:"watch_paths"`
	AutoDeploySpecified    int64              `db:"auto_deploy_specified"`
	AutoDeploy             bool               `db:"auto_deploy"`
	UpdatedAt              sql.NullInt64      `db:"updated_at"`
	WorkspaceID            string             `db:"workspace_id"`
	AppID                  string             `db:"app_id"`
	EnvironmentID          string             `db:"environment_id"`
}

// Updates only the columns whose *_specified flag is 1, preserving all others.
// columns cannot overwrite each other. dockerfile is clearable (narg -> NULL).
//
//	UPDATE app_build_settings t
//	SET
//	    dockerfile = CASE
//	        WHEN CAST(? AS UNSIGNED) = 1 THEN ?
//	        ELSE t.dockerfile
//	    END,
//	    docker_context = CASE
//	        WHEN CAST(? AS UNSIGNED) = 1 THEN ?
//	        ELSE t.docker_context
//	    END,
//	    watch_paths = CASE
//	        WHEN CAST(? AS UNSIGNED) = 1 THEN ?
//	        ELSE t.watch_paths
//	    END,
//	    auto_deploy = CASE
//	        WHEN CAST(? AS UNSIGNED) = 1 THEN ?
//	        ELSE t.auto_deploy
//	    END,
//	    updated_at = ?
//	WHERE workspace_id = ?
//	  AND app_id = ?
//	  AND environment_id = ?
func (q *Queries) UpdateAppBuildSettings(ctx context.Context, db DBTX, arg UpdateAppBuildSettingsParams) error {
	_, err := db.ExecContext(ctx, updateAppBuildSettings,
		arg.DockerfileSpecified,
		arg.Dockerfile,
		arg.DockerContextSpecified,
		arg.DockerContext,
		arg.WatchPathsSpecified,
		arg.WatchPaths,
		arg.AutoDeploySpecified,
		arg.AutoDeploy,
		arg.UpdatedAt,
		arg.WorkspaceID,
		arg.AppID,
		arg.EnvironmentID,
	)
	return err
}
