// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: portal_session_find_revoked_by_external_id.sql

package db

import (
	"context"
	"database/sql"
)

const findPortalSessionsRevokedAtByExternalID = `-- name: FindPortalSessionsRevokedAtByExternalID :many
SELECT pk, id, workspace_id, portal_id, external_id, scopes, exchange_code_hash, exchange_code_expires_at, access_token_hash, access_token_created_at, access_token_expires_at, revoked_at, return_url, created_at FROM portal_sessions
WHERE workspace_id = ?
  AND portal_id = ?
  AND external_id = ?
  AND revoked_at = ?
`

type FindPortalSessionsRevokedAtByExternalIDParams struct {
	WorkspaceID string        `db:"workspace_id"`
	PortalID    string        `db:"portal_id"`
	ExternalID  string        `db:"external_id"`
	RevokedAt   sql.NullInt64 `db:"revoked_at"`
}

// Reads back the rows RevokePortalSessionsByExternalID just revoked, matched by
// the exact revoked_at it wrote, so the caller can write their revoked state
// into the session cache. Run it on the same transaction as the revoke: it then
// returns exactly the rows that update touched.
//
//	SELECT pk, id, workspace_id, portal_id, external_id, scopes, exchange_code_hash, exchange_code_expires_at, access_token_hash, access_token_created_at, access_token_expires_at, revoked_at, return_url, created_at FROM portal_sessions
//	WHERE workspace_id = ?
//	  AND portal_id = ?
//	  AND external_id = ?
//	  AND revoked_at = ?
func (q *Queries) FindPortalSessionsRevokedAtByExternalID(ctx context.Context, db DBTX, arg FindPortalSessionsRevokedAtByExternalIDParams) ([]PortalSession, error) {
	rows, err := db.QueryContext(ctx, findPortalSessionsRevokedAtByExternalID,
		arg.WorkspaceID,
		arg.PortalID,
		arg.ExternalID,
		arg.RevokedAt,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []PortalSession
	for rows.Next() {
		var i PortalSession
		if err := rows.Scan(
			&i.Pk,
			&i.ID,
			&i.WorkspaceID,
			&i.PortalID,
			&i.ExternalID,
			&i.Scopes,
			&i.ExchangeCodeHash,
			&i.ExchangeCodeExpiresAt,
			&i.AccessTokenHash,
			&i.AccessTokenCreatedAt,
			&i.AccessTokenExpiresAt,
			&i.RevokedAt,
			&i.ReturnUrl,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
