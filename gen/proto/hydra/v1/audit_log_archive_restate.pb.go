// Code generated by protoc-gen-go-restate. DO NOT EDIT.
// versions:
// - protoc-gen-go-restate v0.1
// - protoc             (unknown)
// source: hydra/v1/audit_log_archive.proto

package hydrav1

import (
	fmt "fmt"
	sdk_go "github.com/restatedev/sdk-go"
	encoding "github.com/restatedev/sdk-go/encoding"
	ingress "github.com/restatedev/sdk-go/ingress"
)

// AuditLogArchiveServiceClient is the client API for hydra.v1.AuditLogArchiveService service.
//
// AuditLogArchiveService exports audit log rows past their retention to a
// caller-owned S3 bucket as Parquet, then deletes them from ClickHouse.
// This is the sole authority on audit log retention deletion in CH (the
// table has no TTL clause).
//
// Singleton VO keyed "default"; we don't shard. The work is all server-
// side ClickHouse SQL; the worker just orchestrates and journals.
//
// Compliance: the S3 bucket is the durable record of every audit log row
// the system has ever stored. Operators can run audits, restore into a
// separate CH instance, or hand customers a Parquet dump on request.
type AuditLogArchiveServiceClient interface {
	// RunArchive does one archival pass:
	//  1. Compute cutoff = now - safety buffer.
	//  2. INSERT INTO FUNCTION s3(...) SELECT * FROM audit_logs_raw_v1
	//     WHERE expires_at < cutoff. Each pass writes one Parquet file
	//     keyed by cutoff timestamp.
	//  3. ALTER TABLE audit_logs_raw_v1 DELETE WHERE expires_at < cutoff
	//     with mutations_sync = 2 so we know it landed before the
	//     heartbeat fires.
	//
	// If the kill switch (env AUDIT_LOG_ARCHIVE_DISABLED=true) is set, the
	// handler returns immediately with rows_archived = 0. Used to pause
	// archival in incidents.
	RunArchive(opts ...sdk_go.ClientOption) sdk_go.Client[*RunArchiveRequest, *RunArchiveResponse]
}

type auditLogArchiveServiceClient struct {
	ctx     sdk_go.Context
	key     string
	options []sdk_go.ClientOption
}

func NewAuditLogArchiveServiceClient(ctx sdk_go.Context, key string, opts ...sdk_go.ClientOption) AuditLogArchiveServiceClient {
	cOpts := append([]sdk_go.ClientOption{sdk_go.WithProtoJSON}, opts...)
	return &auditLogArchiveServiceClient{
		ctx,
		key,
		cOpts,
	}
}
func (c *auditLogArchiveServiceClient) RunArchive(opts ...sdk_go.ClientOption) sdk_go.Client[*RunArchiveRequest, *RunArchiveResponse] {
	cOpts := c.options
	if len(opts) > 0 {
		cOpts = append(append([]sdk_go.ClientOption{}, cOpts...), opts...)
	}
	return sdk_go.WithRequestType[*RunArchiveRequest](sdk_go.Object[*RunArchiveResponse](c.ctx, "hydra.v1.AuditLogArchiveService", c.key, "RunArchive", cOpts...))
}

// AuditLogArchiveServiceIngressClient is the ingress client API for hydra.v1.AuditLogArchiveService service.
//
// This client is used to call the service from outside of a Restate context.
type AuditLogArchiveServiceIngressClient interface {
	// RunArchive does one archival pass:
	//  1. Compute cutoff = now - safety buffer.
	//  2. INSERT INTO FUNCTION s3(...) SELECT * FROM audit_logs_raw_v1
	//     WHERE expires_at < cutoff. Each pass writes one Parquet file
	//     keyed by cutoff timestamp.
	//  3. ALTER TABLE audit_logs_raw_v1 DELETE WHERE expires_at < cutoff
	//     with mutations_sync = 2 so we know it landed before the
	//     heartbeat fires.
	//
	// If the kill switch (env AUDIT_LOG_ARCHIVE_DISABLED=true) is set, the
	// handler returns immediately with rows_archived = 0. Used to pause
	// archival in incidents.
	RunArchive() ingress.Requester[*RunArchiveRequest, *RunArchiveResponse]
}

type auditLogArchiveServiceIngressClient struct {
	client      *ingress.Client
	serviceName string
	key         string
}

func NewAuditLogArchiveServiceIngressClient(client *ingress.Client, key string) AuditLogArchiveServiceIngressClient {
	return &auditLogArchiveServiceIngressClient{
		client,
		"hydra.v1.AuditLogArchiveService",
		key,
	}
}

func (c *auditLogArchiveServiceIngressClient) RunArchive() ingress.Requester[*RunArchiveRequest, *RunArchiveResponse] {
	codec := encoding.ProtoJSONCodec
	return ingress.NewRequester[*RunArchiveRequest, *RunArchiveResponse](c.client, c.serviceName, "RunArchive", &c.key, &codec)
}

// AuditLogArchiveServiceServer is the server API for hydra.v1.AuditLogArchiveService service.
// All implementations should embed UnimplementedAuditLogArchiveServiceServer
// for forward compatibility.
//
// AuditLogArchiveService exports audit log rows past their retention to a
// caller-owned S3 bucket as Parquet, then deletes them from ClickHouse.
// This is the sole authority on audit log retention deletion in CH (the
// table has no TTL clause).
//
// Singleton VO keyed "default"; we don't shard. The work is all server-
// side ClickHouse SQL; the worker just orchestrates and journals.
//
// Compliance: the S3 bucket is the durable record of every audit log row
// the system has ever stored. Operators can run audits, restore into a
// separate CH instance, or hand customers a Parquet dump on request.
type AuditLogArchiveServiceServer interface {
	// RunArchive does one archival pass:
	//  1. Compute cutoff = now - safety buffer.
	//  2. INSERT INTO FUNCTION s3(...) SELECT * FROM audit_logs_raw_v1
	//     WHERE expires_at < cutoff. Each pass writes one Parquet file
	//     keyed by cutoff timestamp.
	//  3. ALTER TABLE audit_logs_raw_v1 DELETE WHERE expires_at < cutoff
	//     with mutations_sync = 2 so we know it landed before the
	//     heartbeat fires.
	//
	// If the kill switch (env AUDIT_LOG_ARCHIVE_DISABLED=true) is set, the
	// handler returns immediately with rows_archived = 0. Used to pause
	// archival in incidents.
	RunArchive(ctx sdk_go.ObjectContext, req *RunArchiveRequest) (*RunArchiveResponse, error)
}

// UnimplementedAuditLogArchiveServiceServer should be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedAuditLogArchiveServiceServer struct{}

func (UnimplementedAuditLogArchiveServiceServer) RunArchive(ctx sdk_go.ObjectContext, req *RunArchiveRequest) (*RunArchiveResponse, error) {
	return nil, sdk_go.TerminalError(fmt.Errorf("method RunArchive not implemented"), 501)
}
func (UnimplementedAuditLogArchiveServiceServer) testEmbeddedByValue() {}

// UnsafeAuditLogArchiveServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to AuditLogArchiveServiceServer will
// result in compilation errors.
type UnsafeAuditLogArchiveServiceServer interface {
	mustEmbedUnimplementedAuditLogArchiveServiceServer()
}

func NewAuditLogArchiveServiceServer(srv AuditLogArchiveServiceServer, opts ...sdk_go.ServiceDefinitionOption) sdk_go.ServiceDefinition {
	// If the following call panics, it indicates UnimplementedAuditLogArchiveServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	sOpts := append([]sdk_go.ServiceDefinitionOption{sdk_go.WithProtoJSON}, opts...)
	router := sdk_go.NewObject("hydra.v1.AuditLogArchiveService", sOpts...)
	router = router.Handler("RunArchive", sdk_go.NewObjectHandler(srv.RunArchive))
	return router
}
