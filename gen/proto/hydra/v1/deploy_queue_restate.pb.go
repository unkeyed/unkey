// Code generated by protoc-gen-go-restate. DO NOT EDIT.
// versions:
// - protoc-gen-go-restate v0.1
// - protoc             (unknown)
// source: hydra/v1/deploy_queue.proto

package hydrav1

import (
	fmt "fmt"
	sdk_go "github.com/restatedev/sdk-go"
	encoding "github.com/restatedev/sdk-go/encoding"
	ingress "github.com/restatedev/sdk-go/ingress"
)

// DeployQueueServiceClient is the client API for hydra.v1.DeployQueueService service.
//
// DeployQueueService manages per-branch deployment queues. Each instance is
// keyed by a composite key (app_id:branch), ensuring that deployments to the
// same app+branch are serialized while different branches run in parallel.
//
// The queue handles:
//   - Priority ordering (production > preview, FIFO within each tier)
//   - Superseding: new deploys for the same branch cancel older queued/active ones
//   - Build slot coordination with the workspace DeploySchedulerService
//
// Keyed by app_id:branch (composite).
type DeployQueueServiceClient interface {
	// Enqueue adds a deploy request to the queue. Since the key is app_id:branch,
	// all entries in a given queue are for the same branch. New entries supersede
	// all older queued entries (marked as superseded in DB). If an active deploy
	// exists, its Restate invocation is cancelled via the admin API.
	Enqueue(opts ...sdk_go.ClientOption) sdk_go.Client[*QueueEnqueueRequest, *QueueEnqueueResponse]
	// ProcessNext pops the highest-priority item from the queue, requests a build
	// slot from the workspace scheduler, and dispatches the deploy if granted.
	// If no slot is available, the entry is pushed back and the scheduler will
	// call ProcessNext later when a slot opens.
	ProcessNext(opts ...sdk_go.ClientOption) sdk_go.Client[*ProcessNextRequest, *ProcessNextResponse]
	// OnDeployComplete is called by the deploy workflow when it finishes (success,
	// failure, or cancellation). Clears the active deploy, releases the build slot,
	// and triggers ProcessNext for the next queued item.
	OnDeployComplete(opts ...sdk_go.ClientOption) sdk_go.Client[*OnDeployCompleteRequest, *OnDeployCompleteResponse]
}

type deployQueueServiceClient struct {
	ctx     sdk_go.Context
	key     string
	options []sdk_go.ClientOption
}

func NewDeployQueueServiceClient(ctx sdk_go.Context, key string, opts ...sdk_go.ClientOption) DeployQueueServiceClient {
	cOpts := append([]sdk_go.ClientOption{sdk_go.WithProtoJSON}, opts...)
	return &deployQueueServiceClient{
		ctx,
		key,
		cOpts,
	}
}
func (c *deployQueueServiceClient) Enqueue(opts ...sdk_go.ClientOption) sdk_go.Client[*QueueEnqueueRequest, *QueueEnqueueResponse] {
	cOpts := c.options
	if len(opts) > 0 {
		cOpts = append(append([]sdk_go.ClientOption{}, cOpts...), opts...)
	}
	return sdk_go.WithRequestType[*QueueEnqueueRequest](sdk_go.Object[*QueueEnqueueResponse](c.ctx, "hydra.v1.DeployQueueService", c.key, "Enqueue", cOpts...))
}

func (c *deployQueueServiceClient) ProcessNext(opts ...sdk_go.ClientOption) sdk_go.Client[*ProcessNextRequest, *ProcessNextResponse] {
	cOpts := c.options
	if len(opts) > 0 {
		cOpts = append(append([]sdk_go.ClientOption{}, cOpts...), opts...)
	}
	return sdk_go.WithRequestType[*ProcessNextRequest](sdk_go.Object[*ProcessNextResponse](c.ctx, "hydra.v1.DeployQueueService", c.key, "ProcessNext", cOpts...))
}

func (c *deployQueueServiceClient) OnDeployComplete(opts ...sdk_go.ClientOption) sdk_go.Client[*OnDeployCompleteRequest, *OnDeployCompleteResponse] {
	cOpts := c.options
	if len(opts) > 0 {
		cOpts = append(append([]sdk_go.ClientOption{}, cOpts...), opts...)
	}
	return sdk_go.WithRequestType[*OnDeployCompleteRequest](sdk_go.Object[*OnDeployCompleteResponse](c.ctx, "hydra.v1.DeployQueueService", c.key, "OnDeployComplete", cOpts...))
}

// DeployQueueServiceIngressClient is the ingress client API for hydra.v1.DeployQueueService service.
//
// This client is used to call the service from outside of a Restate context.
type DeployQueueServiceIngressClient interface {
	// Enqueue adds a deploy request to the queue. Since the key is app_id:branch,
	// all entries in a given queue are for the same branch. New entries supersede
	// all older queued entries (marked as superseded in DB). If an active deploy
	// exists, its Restate invocation is cancelled via the admin API.
	Enqueue() ingress.Requester[*QueueEnqueueRequest, *QueueEnqueueResponse]
	// ProcessNext pops the highest-priority item from the queue, requests a build
	// slot from the workspace scheduler, and dispatches the deploy if granted.
	// If no slot is available, the entry is pushed back and the scheduler will
	// call ProcessNext later when a slot opens.
	ProcessNext() ingress.Requester[*ProcessNextRequest, *ProcessNextResponse]
	// OnDeployComplete is called by the deploy workflow when it finishes (success,
	// failure, or cancellation). Clears the active deploy, releases the build slot,
	// and triggers ProcessNext for the next queued item.
	OnDeployComplete() ingress.Requester[*OnDeployCompleteRequest, *OnDeployCompleteResponse]
}

type deployQueueServiceIngressClient struct {
	client      *ingress.Client
	serviceName string
	key         string
}

func NewDeployQueueServiceIngressClient(client *ingress.Client, key string) DeployQueueServiceIngressClient {
	return &deployQueueServiceIngressClient{
		client,
		"hydra.v1.DeployQueueService",
		key,
	}
}

func (c *deployQueueServiceIngressClient) Enqueue() ingress.Requester[*QueueEnqueueRequest, *QueueEnqueueResponse] {
	codec := encoding.ProtoJSONCodec
	return ingress.NewRequester[*QueueEnqueueRequest, *QueueEnqueueResponse](c.client, c.serviceName, "Enqueue", &c.key, &codec)
}

func (c *deployQueueServiceIngressClient) ProcessNext() ingress.Requester[*ProcessNextRequest, *ProcessNextResponse] {
	codec := encoding.ProtoJSONCodec
	return ingress.NewRequester[*ProcessNextRequest, *ProcessNextResponse](c.client, c.serviceName, "ProcessNext", &c.key, &codec)
}

func (c *deployQueueServiceIngressClient) OnDeployComplete() ingress.Requester[*OnDeployCompleteRequest, *OnDeployCompleteResponse] {
	codec := encoding.ProtoJSONCodec
	return ingress.NewRequester[*OnDeployCompleteRequest, *OnDeployCompleteResponse](c.client, c.serviceName, "OnDeployComplete", &c.key, &codec)
}

// DeployQueueServiceServer is the server API for hydra.v1.DeployQueueService service.
// All implementations should embed UnimplementedDeployQueueServiceServer
// for forward compatibility.
//
// DeployQueueService manages per-branch deployment queues. Each instance is
// keyed by a composite key (app_id:branch), ensuring that deployments to the
// same app+branch are serialized while different branches run in parallel.
//
// The queue handles:
//   - Priority ordering (production > preview, FIFO within each tier)
//   - Superseding: new deploys for the same branch cancel older queued/active ones
//   - Build slot coordination with the workspace DeploySchedulerService
//
// Keyed by app_id:branch (composite).
type DeployQueueServiceServer interface {
	// Enqueue adds a deploy request to the queue. Since the key is app_id:branch,
	// all entries in a given queue are for the same branch. New entries supersede
	// all older queued entries (marked as superseded in DB). If an active deploy
	// exists, its Restate invocation is cancelled via the admin API.
	Enqueue(ctx sdk_go.ObjectContext, req *QueueEnqueueRequest) (*QueueEnqueueResponse, error)
	// ProcessNext pops the highest-priority item from the queue, requests a build
	// slot from the workspace scheduler, and dispatches the deploy if granted.
	// If no slot is available, the entry is pushed back and the scheduler will
	// call ProcessNext later when a slot opens.
	ProcessNext(ctx sdk_go.ObjectContext, req *ProcessNextRequest) (*ProcessNextResponse, error)
	// OnDeployComplete is called by the deploy workflow when it finishes (success,
	// failure, or cancellation). Clears the active deploy, releases the build slot,
	// and triggers ProcessNext for the next queued item.
	OnDeployComplete(ctx sdk_go.ObjectContext, req *OnDeployCompleteRequest) (*OnDeployCompleteResponse, error)
}

// UnimplementedDeployQueueServiceServer should be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedDeployQueueServiceServer struct{}

func (UnimplementedDeployQueueServiceServer) Enqueue(ctx sdk_go.ObjectContext, req *QueueEnqueueRequest) (*QueueEnqueueResponse, error) {
	return nil, sdk_go.TerminalError(fmt.Errorf("method Enqueue not implemented"), 501)
}
func (UnimplementedDeployQueueServiceServer) ProcessNext(ctx sdk_go.ObjectContext, req *ProcessNextRequest) (*ProcessNextResponse, error) {
	return nil, sdk_go.TerminalError(fmt.Errorf("method ProcessNext not implemented"), 501)
}
func (UnimplementedDeployQueueServiceServer) OnDeployComplete(ctx sdk_go.ObjectContext, req *OnDeployCompleteRequest) (*OnDeployCompleteResponse, error) {
	return nil, sdk_go.TerminalError(fmt.Errorf("method OnDeployComplete not implemented"), 501)
}
func (UnimplementedDeployQueueServiceServer) testEmbeddedByValue() {}

// UnsafeDeployQueueServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to DeployQueueServiceServer will
// result in compilation errors.
type UnsafeDeployQueueServiceServer interface {
	mustEmbedUnimplementedDeployQueueServiceServer()
}

func NewDeployQueueServiceServer(srv DeployQueueServiceServer, opts ...sdk_go.ServiceDefinitionOption) sdk_go.ServiceDefinition {
	// If the following call panics, it indicates UnimplementedDeployQueueServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	sOpts := append([]sdk_go.ServiceDefinitionOption{sdk_go.WithProtoJSON}, opts...)
	router := sdk_go.NewObject("hydra.v1.DeployQueueService", sOpts...)
	router = router.Handler("Enqueue", sdk_go.NewObjectHandler(srv.Enqueue))
	router = router.Handler("ProcessNext", sdk_go.NewObjectHandler(srv.ProcessNext))
	router = router.Handler("OnDeployComplete", sdk_go.NewObjectHandler(srv.OnDeployComplete))
	return router
}
