// Code generated by protoc-gen-go-restate. DO NOT EDIT.
// versions:
// - protoc-gen-go-restate v0.1
// - protoc             (unknown)
// source: hydra/v1/deploy_billing_push.proto

package hydrav1

import (
	fmt "fmt"
	sdk_go "github.com/restatedev/sdk-go"
	encoding "github.com/restatedev/sdk-go/encoding"
	ingress "github.com/restatedev/sdk-go/ingress"
)

// DeployBillingPushServiceClient is the client API for hydra.v1.DeployBillingPushService service.
//
// DeployBillingPushService pushes one workspace's month-to-date Deploy usage
// to the billing provider. The CronService orchestrators (hourly push and
// month-end close) fan out to it, one invocation per workspace.
//
// Keyed by workspace id, so concurrent pushes for the same customer serialize
// (no racing writes to one customer's meters) while different workspaces run
// independently. Each push is its own invocation: it retries on its own
// schedule and, when it cannot succeed, fails as its own visible invocation
// rather than being recorded as a flag inside a batch. One broken workspace
// (deleted customer, frozen test clock) never blocks another's push.
type DeployBillingPushServiceClient interface {
	// PushWorkspaceUsage sends the workspace's month-to-date meter values to the
	// provider. The values are absolute totals and the meters aggregate with
	// formula "last", so retries and overlapping ticks converge on the latest
	// total. Returns an error on failure so the invocation surfaces it.
	PushWorkspaceUsage(opts ...sdk_go.ClientOption) sdk_go.Client[*PushWorkspaceUsageRequest, *PushWorkspaceUsageResponse]
}

type deployBillingPushServiceClient struct {
	ctx     sdk_go.Context
	key     string
	options []sdk_go.ClientOption
}

func NewDeployBillingPushServiceClient(ctx sdk_go.Context, key string, opts ...sdk_go.ClientOption) DeployBillingPushServiceClient {
	cOpts := append([]sdk_go.ClientOption{sdk_go.WithProtoJSON}, opts...)
	return &deployBillingPushServiceClient{
		ctx,
		key,
		cOpts,
	}
}
func (c *deployBillingPushServiceClient) PushWorkspaceUsage(opts ...sdk_go.ClientOption) sdk_go.Client[*PushWorkspaceUsageRequest, *PushWorkspaceUsageResponse] {
	cOpts := c.options
	if len(opts) > 0 {
		cOpts = append(append([]sdk_go.ClientOption{}, cOpts...), opts...)
	}
	return sdk_go.WithRequestType[*PushWorkspaceUsageRequest](sdk_go.Object[*PushWorkspaceUsageResponse](c.ctx, "hydra.v1.DeployBillingPushService", c.key, "PushWorkspaceUsage", cOpts...))
}

// DeployBillingPushServiceIngressClient is the ingress client API for hydra.v1.DeployBillingPushService service.
//
// This client is used to call the service from outside of a Restate context.
type DeployBillingPushServiceIngressClient interface {
	// PushWorkspaceUsage sends the workspace's month-to-date meter values to the
	// provider. The values are absolute totals and the meters aggregate with
	// formula "last", so retries and overlapping ticks converge on the latest
	// total. Returns an error on failure so the invocation surfaces it.
	PushWorkspaceUsage() ingress.Requester[*PushWorkspaceUsageRequest, *PushWorkspaceUsageResponse]
}

type deployBillingPushServiceIngressClient struct {
	client      *ingress.Client
	serviceName string
	key         string
}

func NewDeployBillingPushServiceIngressClient(client *ingress.Client, key string) DeployBillingPushServiceIngressClient {
	return &deployBillingPushServiceIngressClient{
		client,
		"hydra.v1.DeployBillingPushService",
		key,
	}
}

func (c *deployBillingPushServiceIngressClient) PushWorkspaceUsage() ingress.Requester[*PushWorkspaceUsageRequest, *PushWorkspaceUsageResponse] {
	codec := encoding.ProtoJSONCodec
	return ingress.NewRequester[*PushWorkspaceUsageRequest, *PushWorkspaceUsageResponse](c.client, c.serviceName, "PushWorkspaceUsage", &c.key, &codec)
}

// DeployBillingPushServiceServer is the server API for hydra.v1.DeployBillingPushService service.
// All implementations should embed UnimplementedDeployBillingPushServiceServer
// for forward compatibility.
//
// DeployBillingPushService pushes one workspace's month-to-date Deploy usage
// to the billing provider. The CronService orchestrators (hourly push and
// month-end close) fan out to it, one invocation per workspace.
//
// Keyed by workspace id, so concurrent pushes for the same customer serialize
// (no racing writes to one customer's meters) while different workspaces run
// independently. Each push is its own invocation: it retries on its own
// schedule and, when it cannot succeed, fails as its own visible invocation
// rather than being recorded as a flag inside a batch. One broken workspace
// (deleted customer, frozen test clock) never blocks another's push.
type DeployBillingPushServiceServer interface {
	// PushWorkspaceUsage sends the workspace's month-to-date meter values to the
	// provider. The values are absolute totals and the meters aggregate with
	// formula "last", so retries and overlapping ticks converge on the latest
	// total. Returns an error on failure so the invocation surfaces it.
	PushWorkspaceUsage(ctx sdk_go.ObjectContext, req *PushWorkspaceUsageRequest) (*PushWorkspaceUsageResponse, error)
}

// UnimplementedDeployBillingPushServiceServer should be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedDeployBillingPushServiceServer struct{}

func (UnimplementedDeployBillingPushServiceServer) PushWorkspaceUsage(ctx sdk_go.ObjectContext, req *PushWorkspaceUsageRequest) (*PushWorkspaceUsageResponse, error) {
	return nil, sdk_go.TerminalError(fmt.Errorf("method PushWorkspaceUsage not implemented"), 501)
}
func (UnimplementedDeployBillingPushServiceServer) testEmbeddedByValue() {}

// UnsafeDeployBillingPushServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to DeployBillingPushServiceServer will
// result in compilation errors.
type UnsafeDeployBillingPushServiceServer interface {
	mustEmbedUnimplementedDeployBillingPushServiceServer()
}

func NewDeployBillingPushServiceServer(srv DeployBillingPushServiceServer, opts ...sdk_go.ServiceDefinitionOption) sdk_go.ServiceDefinition {
	// If the following call panics, it indicates UnimplementedDeployBillingPushServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	sOpts := append([]sdk_go.ServiceDefinitionOption{sdk_go.WithProtoJSON}, opts...)
	router := sdk_go.NewObject("hydra.v1.DeployBillingPushService", sOpts...)
	router = router.Handler("PushWorkspaceUsage", sdk_go.NewObjectHandler(srv.PushWorkspaceUsage))
	return router
}
