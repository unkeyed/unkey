// Code generated by protoc-gen-go-restate. DO NOT EDIT.
// versions:
// - protoc-gen-go-restate v0.1
// - protoc             (unknown)
// source: hydra/v1/github_webhook.proto

package hydrav1

import (
	fmt "fmt"
	sdk_go "github.com/restatedev/sdk-go"
	encoding "github.com/restatedev/sdk-go/encoding"
	ingress "github.com/restatedev/sdk-go/ingress"
)

// GitHubWebhookServiceClient is the client API for hydra.v1.GitHubWebhookService service.
//
// GitHubWebhookService processes GitHub push events as a Restate virtual object
// keyed by "{installation_id}/{repo_id}". This serializes webhook processing
// per repository, preventing races from concurrent pushes to the same repo.
//
// The HTTP handler validates the signature, parses the payload, and sends
// the request to this service with X-GitHub-Delivery as the idempotency key,
// providing natural deduplication of GitHub's retried deliveries.
type GitHubWebhookServiceClient interface {
	// HandlePush processes a GitHub push event: looks up repo connections,
	// resolves project/environment/app/settings, creates deployment records,
	// and fires off DeployService.Deploy() for each deployment.
	HandlePush(opts ...sdk_go.ClientOption) sdk_go.Client[*HandlePushRequest, *HandlePushResponse]
}

type gitHubWebhookServiceClient struct {
	ctx     sdk_go.Context
	key     string
	options []sdk_go.ClientOption
}

func NewGitHubWebhookServiceClient(ctx sdk_go.Context, key string, opts ...sdk_go.ClientOption) GitHubWebhookServiceClient {
	cOpts := append([]sdk_go.ClientOption{sdk_go.WithProtoJSON}, opts...)
	return &gitHubWebhookServiceClient{
		ctx,
		key,
		cOpts,
	}
}
func (c *gitHubWebhookServiceClient) HandlePush(opts ...sdk_go.ClientOption) sdk_go.Client[*HandlePushRequest, *HandlePushResponse] {
	cOpts := c.options
	if len(opts) > 0 {
		cOpts = append(append([]sdk_go.ClientOption{}, cOpts...), opts...)
	}
	return sdk_go.WithRequestType[*HandlePushRequest](sdk_go.Object[*HandlePushResponse](c.ctx, "hydra.v1.GitHubWebhookService", c.key, "HandlePush", cOpts...))
}

// GitHubWebhookServiceIngressClient is the ingress client API for hydra.v1.GitHubWebhookService service.
//
// This client is used to call the service from outside of a Restate context.
type GitHubWebhookServiceIngressClient interface {
	// HandlePush processes a GitHub push event: looks up repo connections,
	// resolves project/environment/app/settings, creates deployment records,
	// and fires off DeployService.Deploy() for each deployment.
	HandlePush() ingress.Requester[*HandlePushRequest, *HandlePushResponse]
}

type gitHubWebhookServiceIngressClient struct {
	client      *ingress.Client
	serviceName string
	key         string
}

func NewGitHubWebhookServiceIngressClient(client *ingress.Client, key string) GitHubWebhookServiceIngressClient {
	return &gitHubWebhookServiceIngressClient{
		client,
		"hydra.v1.GitHubWebhookService",
		key,
	}
}

func (c *gitHubWebhookServiceIngressClient) HandlePush() ingress.Requester[*HandlePushRequest, *HandlePushResponse] {
	codec := encoding.ProtoJSONCodec
	return ingress.NewRequester[*HandlePushRequest, *HandlePushResponse](c.client, c.serviceName, "HandlePush", &c.key, &codec)
}

// GitHubWebhookServiceServer is the server API for hydra.v1.GitHubWebhookService service.
// All implementations should embed UnimplementedGitHubWebhookServiceServer
// for forward compatibility.
//
// GitHubWebhookService processes GitHub push events as a Restate virtual object
// keyed by "{installation_id}/{repo_id}". This serializes webhook processing
// per repository, preventing races from concurrent pushes to the same repo.
//
// The HTTP handler validates the signature, parses the payload, and sends
// the request to this service with X-GitHub-Delivery as the idempotency key,
// providing natural deduplication of GitHub's retried deliveries.
type GitHubWebhookServiceServer interface {
	// HandlePush processes a GitHub push event: looks up repo connections,
	// resolves project/environment/app/settings, creates deployment records,
	// and fires off DeployService.Deploy() for each deployment.
	HandlePush(ctx sdk_go.ObjectContext, req *HandlePushRequest) (*HandlePushResponse, error)
}

// UnimplementedGitHubWebhookServiceServer should be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedGitHubWebhookServiceServer struct{}

func (UnimplementedGitHubWebhookServiceServer) HandlePush(ctx sdk_go.ObjectContext, req *HandlePushRequest) (*HandlePushResponse, error) {
	return nil, sdk_go.TerminalError(fmt.Errorf("method HandlePush not implemented"), 501)
}
func (UnimplementedGitHubWebhookServiceServer) testEmbeddedByValue() {}

// UnsafeGitHubWebhookServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to GitHubWebhookServiceServer will
// result in compilation errors.
type UnsafeGitHubWebhookServiceServer interface {
	mustEmbedUnimplementedGitHubWebhookServiceServer()
}

func NewGitHubWebhookServiceServer(srv GitHubWebhookServiceServer, opts ...sdk_go.ServiceDefinitionOption) sdk_go.ServiceDefinition {
	// If the following call panics, it indicates UnimplementedGitHubWebhookServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	sOpts := append([]sdk_go.ServiceDefinitionOption{sdk_go.WithProtoJSON}, opts...)
	router := sdk_go.NewObject("hydra.v1.GitHubWebhookService", sOpts...)
	router = router.Handler("HandlePush", sdk_go.NewObjectHandler(srv.HandlePush))
	return router
}
