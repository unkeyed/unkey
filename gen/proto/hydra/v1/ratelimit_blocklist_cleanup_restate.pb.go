// Code generated by protoc-gen-go-restate. DO NOT EDIT.
// versions:
// - protoc-gen-go-restate v0.1
// - protoc             (unknown)
// source: hydra/v1/ratelimit_blocklist_cleanup.proto

package hydrav1

import (
	fmt "fmt"
	sdk_go "github.com/restatedev/sdk-go"
	encoding "github.com/restatedev/sdk-go/encoding"
	ingress "github.com/restatedev/sdk-go/ingress"
)

// RatelimitBlocklistCleanupServiceClient is the client API for hydra.v1.RatelimitBlocklistCleanupService service.
//
// RatelimitBlocklistCleanupService prunes expired rows from
// ratelimit_blocklist so the cross-region propagation table stays bounded.
//
// Stateless — safe to invoke at any cadence. Local in-memory state in the
// ratelimit service is cleaned by its own janitor (every minute), and the
// hot path filters on expires_at > NOW() regardless. This cron exists only
// to keep the MySQL table small enough that BlocklistListActive scans cheap
// and storage stays bounded under sustained abuse.
type RatelimitBlocklistCleanupServiceClient interface {
	// RunCleanup deletes every row where expires_at < NOW(). The hourly
	// schedule means at most one window's worth of expired rows ever lives in
	// the table.
	RunCleanup(opts ...sdk_go.ClientOption) sdk_go.Client[*RunCleanupRequest, *RunCleanupResponse]
}

type ratelimitBlocklistCleanupServiceClient struct {
	ctx     sdk_go.Context
	options []sdk_go.ClientOption
}

func NewRatelimitBlocklistCleanupServiceClient(ctx sdk_go.Context, opts ...sdk_go.ClientOption) RatelimitBlocklistCleanupServiceClient {
	cOpts := append([]sdk_go.ClientOption{sdk_go.WithProtoJSON}, opts...)
	return &ratelimitBlocklistCleanupServiceClient{
		ctx,
		cOpts,
	}
}
func (c *ratelimitBlocklistCleanupServiceClient) RunCleanup(opts ...sdk_go.ClientOption) sdk_go.Client[*RunCleanupRequest, *RunCleanupResponse] {
	cOpts := c.options
	if len(opts) > 0 {
		cOpts = append(append([]sdk_go.ClientOption{}, cOpts...), opts...)
	}
	return sdk_go.WithRequestType[*RunCleanupRequest](sdk_go.Service[*RunCleanupResponse](c.ctx, "hydra.v1.RatelimitBlocklistCleanupService", "RunCleanup", cOpts...))
}

// RatelimitBlocklistCleanupServiceIngressClient is the ingress client API for hydra.v1.RatelimitBlocklistCleanupService service.
//
// This client is used to call the service from outside of a Restate context.
type RatelimitBlocklistCleanupServiceIngressClient interface {
	// RunCleanup deletes every row where expires_at < NOW(). The hourly
	// schedule means at most one window's worth of expired rows ever lives in
	// the table.
	RunCleanup() ingress.Requester[*RunCleanupRequest, *RunCleanupResponse]
}

type ratelimitBlocklistCleanupServiceIngressClient struct {
	client      *ingress.Client
	serviceName string
}

func NewRatelimitBlocklistCleanupServiceIngressClient(client *ingress.Client) RatelimitBlocklistCleanupServiceIngressClient {
	return &ratelimitBlocklistCleanupServiceIngressClient{
		client,
		"hydra.v1.RatelimitBlocklistCleanupService",
	}
}

func (c *ratelimitBlocklistCleanupServiceIngressClient) RunCleanup() ingress.Requester[*RunCleanupRequest, *RunCleanupResponse] {
	codec := encoding.ProtoJSONCodec
	return ingress.NewRequester[*RunCleanupRequest, *RunCleanupResponse](c.client, c.serviceName, "RunCleanup", nil, &codec)
}

// RatelimitBlocklistCleanupServiceServer is the server API for hydra.v1.RatelimitBlocklistCleanupService service.
// All implementations should embed UnimplementedRatelimitBlocklistCleanupServiceServer
// for forward compatibility.
//
// RatelimitBlocklistCleanupService prunes expired rows from
// ratelimit_blocklist so the cross-region propagation table stays bounded.
//
// Stateless — safe to invoke at any cadence. Local in-memory state in the
// ratelimit service is cleaned by its own janitor (every minute), and the
// hot path filters on expires_at > NOW() regardless. This cron exists only
// to keep the MySQL table small enough that BlocklistListActive scans cheap
// and storage stays bounded under sustained abuse.
type RatelimitBlocklistCleanupServiceServer interface {
	// RunCleanup deletes every row where expires_at < NOW(). The hourly
	// schedule means at most one window's worth of expired rows ever lives in
	// the table.
	RunCleanup(ctx sdk_go.Context, req *RunCleanupRequest) (*RunCleanupResponse, error)
}

// UnimplementedRatelimitBlocklistCleanupServiceServer should be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedRatelimitBlocklistCleanupServiceServer struct{}

func (UnimplementedRatelimitBlocklistCleanupServiceServer) RunCleanup(ctx sdk_go.Context, req *RunCleanupRequest) (*RunCleanupResponse, error) {
	return nil, sdk_go.TerminalError(fmt.Errorf("method RunCleanup not implemented"), 501)
}
func (UnimplementedRatelimitBlocklistCleanupServiceServer) testEmbeddedByValue() {}

// UnsafeRatelimitBlocklistCleanupServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to RatelimitBlocklistCleanupServiceServer will
// result in compilation errors.
type UnsafeRatelimitBlocklistCleanupServiceServer interface {
	mustEmbedUnimplementedRatelimitBlocklistCleanupServiceServer()
}

func NewRatelimitBlocklistCleanupServiceServer(srv RatelimitBlocklistCleanupServiceServer, opts ...sdk_go.ServiceDefinitionOption) sdk_go.ServiceDefinition {
	// If the following call panics, it indicates UnimplementedRatelimitBlocklistCleanupServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	sOpts := append([]sdk_go.ServiceDefinitionOption{sdk_go.WithProtoJSON}, opts...)
	router := sdk_go.NewService("hydra.v1.RatelimitBlocklistCleanupService", sOpts...)
	router = router.Handler("RunCleanup", sdk_go.NewServiceHandler(srv.RunCleanup))
	return router
}
