// Code generated by protoc-gen-go-restate. DO NOT EDIT.
// versions:
// - protoc-gen-go-restate v0.1
// - protoc             (unknown)
// source: hydra/v1/audit_log_backfill.proto

package hydrav1

import (
	fmt "fmt"
	sdk_go "github.com/restatedev/sdk-go"
	encoding "github.com/restatedev/sdk-go/encoding"
	ingress "github.com/restatedev/sdk-go/ingress"
)

// AuditLogBackfillServiceClient is the client API for hydra.v1.AuditLogBackfillService service.
//
// AuditLogBackfillService chips through the legacy `audit_log` /
// `audit_log_target` MySQL tables and ships every row into the new
// `audit_logs_raw_v1` ClickHouse table. Runs alongside the dual-write
// drainer until the cursor reaches the end of the legacy table, then idles.
//
// Keyed as a virtual object so Restate serializes invocations: overlapping
// cron triggers (the backfill is slow enough that a tick can straddle the
// next one) queue instead of racing on the cursor. The VO key is always
// "default"; we don't shard this workload because the legacy table's
// primary key on `pk` makes a single forward scan cheaper than per-
// workspace fan-out (which would need a different index).
//
// The cursor lives in VO state under stateKeyLastPK. Resumes after crash
// from wherever the last batch committed.
type AuditLogBackfillServiceClient interface {
	// RunBackfill scans `audit_log` rows by ascending pk past the persisted
	// cursor, joins targets in batched lookups (no N+1), maps to the v1
	// ClickHouse envelope, and writes them to ClickHouse. Loops within the
	// invocation until the page returns less than batchLimit rows, then
	// updates the cursor and returns. CH ReplacingMergeTree dedups on
	// (workspace_id, bucket, time, event_id) so re-running a range after
	// a crash is safe.
	RunBackfill(opts ...sdk_go.ClientOption) sdk_go.Client[*RunBackfillRequest, *RunBackfillResponse]
}

type auditLogBackfillServiceClient struct {
	ctx     sdk_go.Context
	key     string
	options []sdk_go.ClientOption
}

func NewAuditLogBackfillServiceClient(ctx sdk_go.Context, key string, opts ...sdk_go.ClientOption) AuditLogBackfillServiceClient {
	cOpts := append([]sdk_go.ClientOption{sdk_go.WithProtoJSON}, opts...)
	return &auditLogBackfillServiceClient{
		ctx,
		key,
		cOpts,
	}
}
func (c *auditLogBackfillServiceClient) RunBackfill(opts ...sdk_go.ClientOption) sdk_go.Client[*RunBackfillRequest, *RunBackfillResponse] {
	cOpts := c.options
	if len(opts) > 0 {
		cOpts = append(append([]sdk_go.ClientOption{}, cOpts...), opts...)
	}
	return sdk_go.WithRequestType[*RunBackfillRequest](sdk_go.Object[*RunBackfillResponse](c.ctx, "hydra.v1.AuditLogBackfillService", c.key, "RunBackfill", cOpts...))
}

// AuditLogBackfillServiceIngressClient is the ingress client API for hydra.v1.AuditLogBackfillService service.
//
// This client is used to call the service from outside of a Restate context.
type AuditLogBackfillServiceIngressClient interface {
	// RunBackfill scans `audit_log` rows by ascending pk past the persisted
	// cursor, joins targets in batched lookups (no N+1), maps to the v1
	// ClickHouse envelope, and writes them to ClickHouse. Loops within the
	// invocation until the page returns less than batchLimit rows, then
	// updates the cursor and returns. CH ReplacingMergeTree dedups on
	// (workspace_id, bucket, time, event_id) so re-running a range after
	// a crash is safe.
	RunBackfill() ingress.Requester[*RunBackfillRequest, *RunBackfillResponse]
}

type auditLogBackfillServiceIngressClient struct {
	client      *ingress.Client
	serviceName string
	key         string
}

func NewAuditLogBackfillServiceIngressClient(client *ingress.Client, key string) AuditLogBackfillServiceIngressClient {
	return &auditLogBackfillServiceIngressClient{
		client,
		"hydra.v1.AuditLogBackfillService",
		key,
	}
}

func (c *auditLogBackfillServiceIngressClient) RunBackfill() ingress.Requester[*RunBackfillRequest, *RunBackfillResponse] {
	codec := encoding.ProtoJSONCodec
	return ingress.NewRequester[*RunBackfillRequest, *RunBackfillResponse](c.client, c.serviceName, "RunBackfill", &c.key, &codec)
}

// AuditLogBackfillServiceServer is the server API for hydra.v1.AuditLogBackfillService service.
// All implementations should embed UnimplementedAuditLogBackfillServiceServer
// for forward compatibility.
//
// AuditLogBackfillService chips through the legacy `audit_log` /
// `audit_log_target` MySQL tables and ships every row into the new
// `audit_logs_raw_v1` ClickHouse table. Runs alongside the dual-write
// drainer until the cursor reaches the end of the legacy table, then idles.
//
// Keyed as a virtual object so Restate serializes invocations: overlapping
// cron triggers (the backfill is slow enough that a tick can straddle the
// next one) queue instead of racing on the cursor. The VO key is always
// "default"; we don't shard this workload because the legacy table's
// primary key on `pk` makes a single forward scan cheaper than per-
// workspace fan-out (which would need a different index).
//
// The cursor lives in VO state under stateKeyLastPK. Resumes after crash
// from wherever the last batch committed.
type AuditLogBackfillServiceServer interface {
	// RunBackfill scans `audit_log` rows by ascending pk past the persisted
	// cursor, joins targets in batched lookups (no N+1), maps to the v1
	// ClickHouse envelope, and writes them to ClickHouse. Loops within the
	// invocation until the page returns less than batchLimit rows, then
	// updates the cursor and returns. CH ReplacingMergeTree dedups on
	// (workspace_id, bucket, time, event_id) so re-running a range after
	// a crash is safe.
	RunBackfill(ctx sdk_go.ObjectContext, req *RunBackfillRequest) (*RunBackfillResponse, error)
}

// UnimplementedAuditLogBackfillServiceServer should be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedAuditLogBackfillServiceServer struct{}

func (UnimplementedAuditLogBackfillServiceServer) RunBackfill(ctx sdk_go.ObjectContext, req *RunBackfillRequest) (*RunBackfillResponse, error) {
	return nil, sdk_go.TerminalError(fmt.Errorf("method RunBackfill not implemented"), 501)
}
func (UnimplementedAuditLogBackfillServiceServer) testEmbeddedByValue() {}

// UnsafeAuditLogBackfillServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to AuditLogBackfillServiceServer will
// result in compilation errors.
type UnsafeAuditLogBackfillServiceServer interface {
	mustEmbedUnimplementedAuditLogBackfillServiceServer()
}

func NewAuditLogBackfillServiceServer(srv AuditLogBackfillServiceServer, opts ...sdk_go.ServiceDefinitionOption) sdk_go.ServiceDefinition {
	// If the following call panics, it indicates UnimplementedAuditLogBackfillServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	sOpts := append([]sdk_go.ServiceDefinitionOption{sdk_go.WithProtoJSON}, opts...)
	router := sdk_go.NewObject("hydra.v1.AuditLogBackfillService", sOpts...)
	router = router.Handler("RunBackfill", sdk_go.NewObjectHandler(srv.RunBackfill))
	return router
}
