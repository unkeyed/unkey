// Code generated by protoc-gen-go-restate. DO NOT EDIT.
// versions:
// - protoc-gen-go-restate v0.1
// - protoc             (unknown)
// source: hydra/v1/audit_log_export.proto

package hydrav1

import (
	fmt "fmt"
	sdk_go "github.com/restatedev/sdk-go"
	encoding "github.com/restatedev/sdk-go/encoding"
	ingress "github.com/restatedev/sdk-go/ingress"
)

// AuditLogExportServiceClient is the client API for hydra.v1.AuditLogExportService service.
//
// AuditLogExportService ships rows from the MySQL `audit_log` outbox into the
// ClickHouse `audit_logs_raw_v1` table.
//
// Keyed as a virtual object so Restate serializes invocations automatically:
// overlapping triggers (e.g. a slow run straddling the next cron tick) queue
// instead of racing on the same rows. The VO key is always "default"; we don't
// shard this workload.
type AuditLogExportServiceClient interface {
	// RunExport drains the outbox in batches until no unexported rows remain or
	// the batch size is larger than the returned count. Each batch is its own
	// restate.Run so a crash mid-drain only replays the last incomplete batch.
	RunExport(opts ...sdk_go.ClientOption) sdk_go.Client[*RunExportRequest, *RunExportResponse]
}

type auditLogExportServiceClient struct {
	ctx     sdk_go.Context
	key     string
	options []sdk_go.ClientOption
}

func NewAuditLogExportServiceClient(ctx sdk_go.Context, key string, opts ...sdk_go.ClientOption) AuditLogExportServiceClient {
	cOpts := append([]sdk_go.ClientOption{sdk_go.WithProtoJSON}, opts...)
	return &auditLogExportServiceClient{
		ctx,
		key,
		cOpts,
	}
}
func (c *auditLogExportServiceClient) RunExport(opts ...sdk_go.ClientOption) sdk_go.Client[*RunExportRequest, *RunExportResponse] {
	cOpts := c.options
	if len(opts) > 0 {
		cOpts = append(append([]sdk_go.ClientOption{}, cOpts...), opts...)
	}
	return sdk_go.WithRequestType[*RunExportRequest](sdk_go.Object[*RunExportResponse](c.ctx, "hydra.v1.AuditLogExportService", c.key, "RunExport", cOpts...))
}

// AuditLogExportServiceIngressClient is the ingress client API for hydra.v1.AuditLogExportService service.
//
// This client is used to call the service from outside of a Restate context.
type AuditLogExportServiceIngressClient interface {
	// RunExport drains the outbox in batches until no unexported rows remain or
	// the batch size is larger than the returned count. Each batch is its own
	// restate.Run so a crash mid-drain only replays the last incomplete batch.
	RunExport() ingress.Requester[*RunExportRequest, *RunExportResponse]
}

type auditLogExportServiceIngressClient struct {
	client      *ingress.Client
	serviceName string
	key         string
}

func NewAuditLogExportServiceIngressClient(client *ingress.Client, key string) AuditLogExportServiceIngressClient {
	return &auditLogExportServiceIngressClient{
		client,
		"hydra.v1.AuditLogExportService",
		key,
	}
}

func (c *auditLogExportServiceIngressClient) RunExport() ingress.Requester[*RunExportRequest, *RunExportResponse] {
	codec := encoding.ProtoJSONCodec
	return ingress.NewRequester[*RunExportRequest, *RunExportResponse](c.client, c.serviceName, "RunExport", &c.key, &codec)
}

// AuditLogExportServiceServer is the server API for hydra.v1.AuditLogExportService service.
// All implementations should embed UnimplementedAuditLogExportServiceServer
// for forward compatibility.
//
// AuditLogExportService ships rows from the MySQL `audit_log` outbox into the
// ClickHouse `audit_logs_raw_v1` table.
//
// Keyed as a virtual object so Restate serializes invocations automatically:
// overlapping triggers (e.g. a slow run straddling the next cron tick) queue
// instead of racing on the same rows. The VO key is always "default"; we don't
// shard this workload.
type AuditLogExportServiceServer interface {
	// RunExport drains the outbox in batches until no unexported rows remain or
	// the batch size is larger than the returned count. Each batch is its own
	// restate.Run so a crash mid-drain only replays the last incomplete batch.
	RunExport(ctx sdk_go.ObjectContext, req *RunExportRequest) (*RunExportResponse, error)
}

// UnimplementedAuditLogExportServiceServer should be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedAuditLogExportServiceServer struct{}

func (UnimplementedAuditLogExportServiceServer) RunExport(ctx sdk_go.ObjectContext, req *RunExportRequest) (*RunExportResponse, error) {
	return nil, sdk_go.TerminalError(fmt.Errorf("method RunExport not implemented"), 501)
}
func (UnimplementedAuditLogExportServiceServer) testEmbeddedByValue() {}

// UnsafeAuditLogExportServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to AuditLogExportServiceServer will
// result in compilation errors.
type UnsafeAuditLogExportServiceServer interface {
	mustEmbedUnimplementedAuditLogExportServiceServer()
}

func NewAuditLogExportServiceServer(srv AuditLogExportServiceServer, opts ...sdk_go.ServiceDefinitionOption) sdk_go.ServiceDefinition {
	// If the following call panics, it indicates UnimplementedAuditLogExportServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	sOpts := append([]sdk_go.ServiceDefinitionOption{sdk_go.WithProtoJSON}, opts...)
	router := sdk_go.NewObject("hydra.v1.AuditLogExportService", sOpts...)
	router = router.Handler("RunExport", sdk_go.NewObjectHandler(srv.RunExport))
	return router
}
