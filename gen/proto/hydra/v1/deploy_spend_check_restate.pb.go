// Code generated by protoc-gen-go-restate. DO NOT EDIT.
// versions:
// - protoc-gen-go-restate v0.1
// - protoc             (unknown)
// source: hydra/v1/deploy_spend_check.proto

package hydrav1

import (
	fmt "fmt"
	sdk_go "github.com/restatedev/sdk-go"
	encoding "github.com/restatedev/sdk-go/encoding"
	ingress "github.com/restatedev/sdk-go/ingress"
)

// DeploySpendCheckServiceClient is the client API for hydra.v1.DeploySpendCheckService service.
//
// DeploySpendCheckService evaluates one workspace's Compute spend against its
// configured budget. The RunDeploySpendCheck orchestrator fans out to it, one
// invocation per budgeted workspace.
//
// Keyed by workspace id so a customer's checks serialize (the notify high-water
// mark is read-modify-written in VO state) and a broken workspace fails in
// isolation. Holding the high-water mark in VO state, keyed by period, is how
// the check knows whether an alert was already sent: it emails only when the
// crossed threshold exceeds the mark, then raises it. A new period is a new key
// state, so the marks reset for free at the period roll.
type DeploySpendCheckServiceClient interface {
	// CheckWorkspaceSpend prices the workspace's month-to-date Deploy usage from
	// ClickHouse, computes the net-of-credit overage against the budget, and
	// sends a budget alert for each 50/75/100% threshold newly crossed this
	// period (at most one email per tick, for the highest crossing). Idempotent
	// per period via the VO high-water mark.
	CheckWorkspaceSpend(opts ...sdk_go.ClientOption) sdk_go.Client[*CheckWorkspaceSpendRequest, *CheckWorkspaceSpendResponse]
}

type deploySpendCheckServiceClient struct {
	ctx     sdk_go.Context
	key     string
	options []sdk_go.ClientOption
}

func NewDeploySpendCheckServiceClient(ctx sdk_go.Context, key string, opts ...sdk_go.ClientOption) DeploySpendCheckServiceClient {
	cOpts := append([]sdk_go.ClientOption{sdk_go.WithProtoJSON}, opts...)
	return &deploySpendCheckServiceClient{
		ctx,
		key,
		cOpts,
	}
}
func (c *deploySpendCheckServiceClient) CheckWorkspaceSpend(opts ...sdk_go.ClientOption) sdk_go.Client[*CheckWorkspaceSpendRequest, *CheckWorkspaceSpendResponse] {
	cOpts := c.options
	if len(opts) > 0 {
		cOpts = append(append([]sdk_go.ClientOption{}, cOpts...), opts...)
	}
	return sdk_go.WithRequestType[*CheckWorkspaceSpendRequest](sdk_go.Object[*CheckWorkspaceSpendResponse](c.ctx, "hydra.v1.DeploySpendCheckService", c.key, "CheckWorkspaceSpend", cOpts...))
}

// DeploySpendCheckServiceIngressClient is the ingress client API for hydra.v1.DeploySpendCheckService service.
//
// This client is used to call the service from outside of a Restate context.
type DeploySpendCheckServiceIngressClient interface {
	// CheckWorkspaceSpend prices the workspace's month-to-date Deploy usage from
	// ClickHouse, computes the net-of-credit overage against the budget, and
	// sends a budget alert for each 50/75/100% threshold newly crossed this
	// period (at most one email per tick, for the highest crossing). Idempotent
	// per period via the VO high-water mark.
	CheckWorkspaceSpend() ingress.Requester[*CheckWorkspaceSpendRequest, *CheckWorkspaceSpendResponse]
}

type deploySpendCheckServiceIngressClient struct {
	client      *ingress.Client
	serviceName string
	key         string
}

func NewDeploySpendCheckServiceIngressClient(client *ingress.Client, key string) DeploySpendCheckServiceIngressClient {
	return &deploySpendCheckServiceIngressClient{
		client,
		"hydra.v1.DeploySpendCheckService",
		key,
	}
}

func (c *deploySpendCheckServiceIngressClient) CheckWorkspaceSpend() ingress.Requester[*CheckWorkspaceSpendRequest, *CheckWorkspaceSpendResponse] {
	codec := encoding.ProtoJSONCodec
	return ingress.NewRequester[*CheckWorkspaceSpendRequest, *CheckWorkspaceSpendResponse](c.client, c.serviceName, "CheckWorkspaceSpend", &c.key, &codec)
}

// DeploySpendCheckServiceServer is the server API for hydra.v1.DeploySpendCheckService service.
// All implementations should embed UnimplementedDeploySpendCheckServiceServer
// for forward compatibility.
//
// DeploySpendCheckService evaluates one workspace's Compute spend against its
// configured budget. The RunDeploySpendCheck orchestrator fans out to it, one
// invocation per budgeted workspace.
//
// Keyed by workspace id so a customer's checks serialize (the notify high-water
// mark is read-modify-written in VO state) and a broken workspace fails in
// isolation. Holding the high-water mark in VO state, keyed by period, is how
// the check knows whether an alert was already sent: it emails only when the
// crossed threshold exceeds the mark, then raises it. A new period is a new key
// state, so the marks reset for free at the period roll.
type DeploySpendCheckServiceServer interface {
	// CheckWorkspaceSpend prices the workspace's month-to-date Deploy usage from
	// ClickHouse, computes the net-of-credit overage against the budget, and
	// sends a budget alert for each 50/75/100% threshold newly crossed this
	// period (at most one email per tick, for the highest crossing). Idempotent
	// per period via the VO high-water mark.
	CheckWorkspaceSpend(ctx sdk_go.ObjectContext, req *CheckWorkspaceSpendRequest) (*CheckWorkspaceSpendResponse, error)
}

// UnimplementedDeploySpendCheckServiceServer should be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedDeploySpendCheckServiceServer struct{}

func (UnimplementedDeploySpendCheckServiceServer) CheckWorkspaceSpend(ctx sdk_go.ObjectContext, req *CheckWorkspaceSpendRequest) (*CheckWorkspaceSpendResponse, error) {
	return nil, sdk_go.TerminalError(fmt.Errorf("method CheckWorkspaceSpend not implemented"), 501)
}
func (UnimplementedDeploySpendCheckServiceServer) testEmbeddedByValue() {}

// UnsafeDeploySpendCheckServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to DeploySpendCheckServiceServer will
// result in compilation errors.
type UnsafeDeploySpendCheckServiceServer interface {
	mustEmbedUnimplementedDeploySpendCheckServiceServer()
}

func NewDeploySpendCheckServiceServer(srv DeploySpendCheckServiceServer, opts ...sdk_go.ServiceDefinitionOption) sdk_go.ServiceDefinition {
	// If the following call panics, it indicates UnimplementedDeploySpendCheckServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	sOpts := append([]sdk_go.ServiceDefinitionOption{sdk_go.WithProtoJSON}, opts...)
	router := sdk_go.NewObject("hydra.v1.DeploySpendCheckService", sOpts...)
	router = router.Handler("CheckWorkspaceSpend", sdk_go.NewObjectHandler(srv.CheckWorkspaceSpend))
	return router
}
