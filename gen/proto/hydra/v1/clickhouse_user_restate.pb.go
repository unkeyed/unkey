// Code generated by protoc-gen-go-restate. DO NOT EDIT.
// versions:
// - protoc-gen-go-restate v0.1
// - protoc             (unknown)
// source: hydra/v1/clickhouse_user.proto

package hydrav1

import (
	fmt "fmt"
	sdk_go "github.com/restatedev/sdk-go"
	encoding "github.com/restatedev/sdk-go/encoding"
	ingress "github.com/restatedev/sdk-go/ingress"
)

// ClickhouseUserServiceClient is the client API for hydra.v1.ClickhouseUserService service.
//
// ClickhouseUserService manages ClickHouse user provisioning for workspaces.
//
// This service creates and configures ClickHouse users with appropriate
// permissions, quotas, and row-level security policies for analytics access.
// It uses the vault API for password encryption and stores credentials in MySQL.
type ClickhouseUserServiceClient interface {
	// ConfigureUser creates or updates a ClickHouse user for a workspace.
	//
	// Key: workspace_id (prevents concurrent user provisioning for the same workspace)
	//
	// For new users:
	// - Generates a secure password
	// - Encrypts password via vault API
	// - Stores credentials in MySQL
	// - Creates ClickHouse user with permissions, quotas, and row policies
	//
	// For existing users:
	// - Preserves the existing password
	// - Updates quotas and settings as specified
	// - Reapplies ClickHouse permissions and row policies
	ConfigureUser(opts ...sdk_go.ClientOption) sdk_go.Client[*ConfigureUserRequest, *ConfigureUserResponse]
}

type clickhouseUserServiceClient struct {
	ctx     sdk_go.Context
	key     string
	options []sdk_go.ClientOption
}

func NewClickhouseUserServiceClient(ctx sdk_go.Context, key string, opts ...sdk_go.ClientOption) ClickhouseUserServiceClient {
	cOpts := append([]sdk_go.ClientOption{sdk_go.WithProtoJSON}, opts...)
	return &clickhouseUserServiceClient{
		ctx,
		key,
		cOpts,
	}
}
func (c *clickhouseUserServiceClient) ConfigureUser(opts ...sdk_go.ClientOption) sdk_go.Client[*ConfigureUserRequest, *ConfigureUserResponse] {
	cOpts := c.options
	if len(opts) > 0 {
		cOpts = append(append([]sdk_go.ClientOption{}, cOpts...), opts...)
	}
	return sdk_go.WithRequestType[*ConfigureUserRequest](sdk_go.Object[*ConfigureUserResponse](c.ctx, "hydra.v1.ClickhouseUserService", c.key, "ConfigureUser", cOpts...))
}

// ClickhouseUserServiceIngressClient is the ingress client API for hydra.v1.ClickhouseUserService service.
//
// This client is used to call the service from outside of a Restate context.
type ClickhouseUserServiceIngressClient interface {
	// ConfigureUser creates or updates a ClickHouse user for a workspace.
	//
	// Key: workspace_id (prevents concurrent user provisioning for the same workspace)
	//
	// For new users:
	// - Generates a secure password
	// - Encrypts password via vault API
	// - Stores credentials in MySQL
	// - Creates ClickHouse user with permissions, quotas, and row policies
	//
	// For existing users:
	// - Preserves the existing password
	// - Updates quotas and settings as specified
	// - Reapplies ClickHouse permissions and row policies
	ConfigureUser() ingress.Requester[*ConfigureUserRequest, *ConfigureUserResponse]
}

type clickhouseUserServiceIngressClient struct {
	client      *ingress.Client
	serviceName string
	key         string
}

func NewClickhouseUserServiceIngressClient(client *ingress.Client, key string) ClickhouseUserServiceIngressClient {
	return &clickhouseUserServiceIngressClient{
		client,
		"hydra.v1.ClickhouseUserService",
		key,
	}
}

func (c *clickhouseUserServiceIngressClient) ConfigureUser() ingress.Requester[*ConfigureUserRequest, *ConfigureUserResponse] {
	codec := encoding.ProtoJSONCodec
	return ingress.NewRequester[*ConfigureUserRequest, *ConfigureUserResponse](c.client, c.serviceName, "ConfigureUser", &c.key, &codec)
}

// ClickhouseUserServiceServer is the server API for hydra.v1.ClickhouseUserService service.
// All implementations should embed UnimplementedClickhouseUserServiceServer
// for forward compatibility.
//
// ClickhouseUserService manages ClickHouse user provisioning for workspaces.
//
// This service creates and configures ClickHouse users with appropriate
// permissions, quotas, and row-level security policies for analytics access.
// It uses the vault API for password encryption and stores credentials in MySQL.
type ClickhouseUserServiceServer interface {
	// ConfigureUser creates or updates a ClickHouse user for a workspace.
	//
	// Key: workspace_id (prevents concurrent user provisioning for the same workspace)
	//
	// For new users:
	// - Generates a secure password
	// - Encrypts password via vault API
	// - Stores credentials in MySQL
	// - Creates ClickHouse user with permissions, quotas, and row policies
	//
	// For existing users:
	// - Preserves the existing password
	// - Updates quotas and settings as specified
	// - Reapplies ClickHouse permissions and row policies
	ConfigureUser(ctx sdk_go.ObjectContext, req *ConfigureUserRequest) (*ConfigureUserResponse, error)
}

// UnimplementedClickhouseUserServiceServer should be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedClickhouseUserServiceServer struct{}

func (UnimplementedClickhouseUserServiceServer) ConfigureUser(ctx sdk_go.ObjectContext, req *ConfigureUserRequest) (*ConfigureUserResponse, error) {
	return nil, sdk_go.TerminalError(fmt.Errorf("method ConfigureUser not implemented"), 501)
}
func (UnimplementedClickhouseUserServiceServer) testEmbeddedByValue() {}

// UnsafeClickhouseUserServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to ClickhouseUserServiceServer will
// result in compilation errors.
type UnsafeClickhouseUserServiceServer interface {
	mustEmbedUnimplementedClickhouseUserServiceServer()
}

func NewClickhouseUserServiceServer(srv ClickhouseUserServiceServer, opts ...sdk_go.ServiceDefinitionOption) sdk_go.ServiceDefinition {
	// If the following call panics, it indicates UnimplementedClickhouseUserServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	sOpts := append([]sdk_go.ServiceDefinitionOption{sdk_go.WithProtoJSON}, opts...)
	router := sdk_go.NewObject("hydra.v1.ClickhouseUserService", sOpts...)
	router = router.Handler("ConfigureUser", sdk_go.NewObjectHandler(srv.ConfigureUser))
	return router
}
