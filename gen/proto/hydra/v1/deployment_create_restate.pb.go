// Code generated by protoc-gen-go-restate. DO NOT EDIT.
// versions:
// - protoc-gen-go-restate v0.1
// - protoc             (unknown)
// source: hydra/v1/deployment_create.proto

package hydrav1

import (
	fmt "fmt"
	sdk_go "github.com/restatedev/sdk-go"
	encoding "github.com/restatedev/sdk-go/encoding"
	ingress "github.com/restatedev/sdk-go/ingress"
)

// DeploymentCreateServiceClient is the client API for hydra.v1.DeploymentCreateService service.
//
// DeploymentCreateService owns the durable part of a deployment create: the
// row insert with its audit log, the DeployService send, the invocation-id
// write, and the sibling dedup. Ctrl runs the gates and resolves the build
// source before the call, so a request that reaches this service is already
// authorized and fully specified.
//
// A plain service, not a virtual object: creates for different deployments
// have nothing to serialize on. Deduplication of caller retries happens at
// the invocation layer instead: ctrl calls Create with an idempotency key
// derived from (workspace id, app id, environment id, caller key), so a
// retry attaches to the original invocation and receives its journaled
// response. Durable execution completes every accepted create: it cannot
// die between the insert and the send.
type DeploymentCreateServiceClient interface {
	// Create inserts the deployment row (with its create audit in the same
	// transaction), starts the Deploy workflow, records the invocation id, and
	// cancels superseded queued siblings. A target deleted between ctrl's
	// checks and execution fails terminally; transient failures retry.
	Create(opts ...sdk_go.ClientOption) sdk_go.Client[*DeploymentCreateRequest, *DeploymentCreateResponse]
}

type deploymentCreateServiceClient struct {
	ctx     sdk_go.Context
	options []sdk_go.ClientOption
}

func NewDeploymentCreateServiceClient(ctx sdk_go.Context, opts ...sdk_go.ClientOption) DeploymentCreateServiceClient {
	cOpts := append([]sdk_go.ClientOption{sdk_go.WithProtoJSON}, opts...)
	return &deploymentCreateServiceClient{
		ctx,
		cOpts,
	}
}
func (c *deploymentCreateServiceClient) Create(opts ...sdk_go.ClientOption) sdk_go.Client[*DeploymentCreateRequest, *DeploymentCreateResponse] {
	cOpts := c.options
	if len(opts) > 0 {
		cOpts = append(append([]sdk_go.ClientOption{}, cOpts...), opts...)
	}
	return sdk_go.WithRequestType[*DeploymentCreateRequest](sdk_go.Service[*DeploymentCreateResponse](c.ctx, "hydra.v1.DeploymentCreateService", "Create", cOpts...))
}

// DeploymentCreateServiceIngressClient is the ingress client API for hydra.v1.DeploymentCreateService service.
//
// This client is used to call the service from outside of a Restate context.
type DeploymentCreateServiceIngressClient interface {
	// Create inserts the deployment row (with its create audit in the same
	// transaction), starts the Deploy workflow, records the invocation id, and
	// cancels superseded queued siblings. A target deleted between ctrl's
	// checks and execution fails terminally; transient failures retry.
	Create() ingress.Requester[*DeploymentCreateRequest, *DeploymentCreateResponse]
}

type deploymentCreateServiceIngressClient struct {
	client      *ingress.Client
	serviceName string
}

func NewDeploymentCreateServiceIngressClient(client *ingress.Client) DeploymentCreateServiceIngressClient {
	return &deploymentCreateServiceIngressClient{
		client,
		"hydra.v1.DeploymentCreateService",
	}
}

func (c *deploymentCreateServiceIngressClient) Create() ingress.Requester[*DeploymentCreateRequest, *DeploymentCreateResponse] {
	codec := encoding.ProtoJSONCodec
	return ingress.NewRequester[*DeploymentCreateRequest, *DeploymentCreateResponse](c.client, c.serviceName, "Create", nil, &codec)
}

// DeploymentCreateServiceServer is the server API for hydra.v1.DeploymentCreateService service.
// All implementations should embed UnimplementedDeploymentCreateServiceServer
// for forward compatibility.
//
// DeploymentCreateService owns the durable part of a deployment create: the
// row insert with its audit log, the DeployService send, the invocation-id
// write, and the sibling dedup. Ctrl runs the gates and resolves the build
// source before the call, so a request that reaches this service is already
// authorized and fully specified.
//
// A plain service, not a virtual object: creates for different deployments
// have nothing to serialize on. Deduplication of caller retries happens at
// the invocation layer instead: ctrl calls Create with an idempotency key
// derived from (workspace id, app id, environment id, caller key), so a
// retry attaches to the original invocation and receives its journaled
// response. Durable execution completes every accepted create: it cannot
// die between the insert and the send.
type DeploymentCreateServiceServer interface {
	// Create inserts the deployment row (with its create audit in the same
	// transaction), starts the Deploy workflow, records the invocation id, and
	// cancels superseded queued siblings. A target deleted between ctrl's
	// checks and execution fails terminally; transient failures retry.
	Create(ctx sdk_go.Context, req *DeploymentCreateRequest) (*DeploymentCreateResponse, error)
}

// UnimplementedDeploymentCreateServiceServer should be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedDeploymentCreateServiceServer struct{}

func (UnimplementedDeploymentCreateServiceServer) Create(ctx sdk_go.Context, req *DeploymentCreateRequest) (*DeploymentCreateResponse, error) {
	return nil, sdk_go.TerminalError(fmt.Errorf("method Create not implemented"), 501)
}
func (UnimplementedDeploymentCreateServiceServer) testEmbeddedByValue() {}

// UnsafeDeploymentCreateServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to DeploymentCreateServiceServer will
// result in compilation errors.
type UnsafeDeploymentCreateServiceServer interface {
	mustEmbedUnimplementedDeploymentCreateServiceServer()
}

func NewDeploymentCreateServiceServer(srv DeploymentCreateServiceServer, opts ...sdk_go.ServiceDefinitionOption) sdk_go.ServiceDefinition {
	// If the following call panics, it indicates UnimplementedDeploymentCreateServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	sOpts := append([]sdk_go.ServiceDefinitionOption{sdk_go.WithProtoJSON}, opts...)
	router := sdk_go.NewService("hydra.v1.DeploymentCreateService", sOpts...)
	router = router.Handler("Create", sdk_go.NewServiceHandler(srv.Create))
	return router
}
