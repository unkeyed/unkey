// Code generated by protoc-gen-go-restate. DO NOT EDIT.
// versions:
// - protoc-gen-go-restate v0.1
// - protoc             (unknown)
// source: hydra/v1/ratelimit_global_counters_cleanup.proto

package hydrav1

import (
	fmt "fmt"
	sdk_go "github.com/restatedev/sdk-go"
	encoding "github.com/restatedev/sdk-go/encoding"
	ingress "github.com/restatedev/sdk-go/ingress"
)

// RatelimitGlobalCountersCleanupServiceClient is the client API for hydra.v1.RatelimitGlobalCountersCleanupService service.
//
// RatelimitGlobalCountersCleanupService prunes expired rows from
// ratelimit_global_counters so the cross-region propagation table stays bounded.
//
// Stateless — safe to invoke at any cadence. Local in-memory state in the
// ratelimit service is cleaned by its own janitor (every minute), and the
// hot path filters on expires_at > NOW() regardless. This cron exists only
// to keep the MySQL table small enough that GlobalCountersImported scans cheap
// and storage stays bounded under sustained abuse.
type RatelimitGlobalCountersCleanupServiceClient interface {
	// RunCleanup deletes every row where expires_at < NOW(). The hourly
	// schedule means at most one window's worth of expired rows ever lives in
	// the table.
	RunCleanup(opts ...sdk_go.ClientOption) sdk_go.Client[*RunCleanupRequest, *RunCleanupResponse]
}

type ratelimitGlobalCountersCleanupServiceClient struct {
	ctx     sdk_go.Context
	options []sdk_go.ClientOption
}

func NewRatelimitGlobalCountersCleanupServiceClient(ctx sdk_go.Context, opts ...sdk_go.ClientOption) RatelimitGlobalCountersCleanupServiceClient {
	cOpts := append([]sdk_go.ClientOption{sdk_go.WithProtoJSON}, opts...)
	return &ratelimitGlobalCountersCleanupServiceClient{
		ctx,
		cOpts,
	}
}
func (c *ratelimitGlobalCountersCleanupServiceClient) RunCleanup(opts ...sdk_go.ClientOption) sdk_go.Client[*RunCleanupRequest, *RunCleanupResponse] {
	cOpts := c.options
	if len(opts) > 0 {
		cOpts = append(append([]sdk_go.ClientOption{}, cOpts...), opts...)
	}
	return sdk_go.WithRequestType[*RunCleanupRequest](sdk_go.Service[*RunCleanupResponse](c.ctx, "hydra.v1.RatelimitGlobalCountersCleanupService", "RunCleanup", cOpts...))
}

// RatelimitGlobalCountersCleanupServiceIngressClient is the ingress client API for hydra.v1.RatelimitGlobalCountersCleanupService service.
//
// This client is used to call the service from outside of a Restate context.
type RatelimitGlobalCountersCleanupServiceIngressClient interface {
	// RunCleanup deletes every row where expires_at < NOW(). The hourly
	// schedule means at most one window's worth of expired rows ever lives in
	// the table.
	RunCleanup() ingress.Requester[*RunCleanupRequest, *RunCleanupResponse]
}

type ratelimitGlobalCountersCleanupServiceIngressClient struct {
	client      *ingress.Client
	serviceName string
}

func NewRatelimitGlobalCountersCleanupServiceIngressClient(client *ingress.Client) RatelimitGlobalCountersCleanupServiceIngressClient {
	return &ratelimitGlobalCountersCleanupServiceIngressClient{
		client,
		"hydra.v1.RatelimitGlobalCountersCleanupService",
	}
}

func (c *ratelimitGlobalCountersCleanupServiceIngressClient) RunCleanup() ingress.Requester[*RunCleanupRequest, *RunCleanupResponse] {
	codec := encoding.ProtoJSONCodec
	return ingress.NewRequester[*RunCleanupRequest, *RunCleanupResponse](c.client, c.serviceName, "RunCleanup", nil, &codec)
}

// RatelimitGlobalCountersCleanupServiceServer is the server API for hydra.v1.RatelimitGlobalCountersCleanupService service.
// All implementations should embed UnimplementedRatelimitGlobalCountersCleanupServiceServer
// for forward compatibility.
//
// RatelimitGlobalCountersCleanupService prunes expired rows from
// ratelimit_global_counters so the cross-region propagation table stays bounded.
//
// Stateless — safe to invoke at any cadence. Local in-memory state in the
// ratelimit service is cleaned by its own janitor (every minute), and the
// hot path filters on expires_at > NOW() regardless. This cron exists only
// to keep the MySQL table small enough that GlobalCountersImported scans cheap
// and storage stays bounded under sustained abuse.
type RatelimitGlobalCountersCleanupServiceServer interface {
	// RunCleanup deletes every row where expires_at < NOW(). The hourly
	// schedule means at most one window's worth of expired rows ever lives in
	// the table.
	RunCleanup(ctx sdk_go.Context, req *RunCleanupRequest) (*RunCleanupResponse, error)
}

// UnimplementedRatelimitGlobalCountersCleanupServiceServer should be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedRatelimitGlobalCountersCleanupServiceServer struct{}

func (UnimplementedRatelimitGlobalCountersCleanupServiceServer) RunCleanup(ctx sdk_go.Context, req *RunCleanupRequest) (*RunCleanupResponse, error) {
	return nil, sdk_go.TerminalError(fmt.Errorf("method RunCleanup not implemented"), 501)
}
func (UnimplementedRatelimitGlobalCountersCleanupServiceServer) testEmbeddedByValue() {}

// UnsafeRatelimitGlobalCountersCleanupServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to RatelimitGlobalCountersCleanupServiceServer will
// result in compilation errors.
type UnsafeRatelimitGlobalCountersCleanupServiceServer interface {
	mustEmbedUnimplementedRatelimitGlobalCountersCleanupServiceServer()
}

func NewRatelimitGlobalCountersCleanupServiceServer(srv RatelimitGlobalCountersCleanupServiceServer, opts ...sdk_go.ServiceDefinitionOption) sdk_go.ServiceDefinition {
	// If the following call panics, it indicates UnimplementedRatelimitGlobalCountersCleanupServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	sOpts := append([]sdk_go.ServiceDefinitionOption{sdk_go.WithProtoJSON}, opts...)
	router := sdk_go.NewService("hydra.v1.RatelimitGlobalCountersCleanupService", sOpts...)
	router = router.Handler("RunCleanup", sdk_go.NewServiceHandler(srv.RunCleanup))
	return router
}
