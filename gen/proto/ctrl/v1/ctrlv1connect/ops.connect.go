// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: ctrl/v1/ops.proto

package ctrlv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/unkeyed/unkey/gen/proto/ctrl/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// OpsServiceName is the fully-qualified name of the OpsService service.
	OpsServiceName = "ctrl.v1.OpsService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// OpsServiceRebuildDeploymentProcedure is the fully-qualified name of the OpsService's
	// RebuildDeployment RPC.
	OpsServiceRebuildDeploymentProcedure = "/ctrl.v1.OpsService/RebuildDeployment"
)

// OpsServiceClient is a client for the ctrl.v1.OpsService service.
type OpsServiceClient interface {
	// RebuildDeployment creates a new deployment that clones the given source
	// deployment's project, app, and environment, then kicks off the deploy
	// workflow. The new deployment inherits the app's *current* runtime
	// settings and env vars — config drift since the source applies.
	//
	// Source resolution:
	//  1. If the source has a git_commit_sha AND the app has a github repo
	//     connection, rebuild from the pinned SHA.
	//  2. Otherwise reuse the source deployment's docker image verbatim.
	//  3. If neither is available, the RPC fails.
	//
	// Guardrail (unless `force` is true): no newer active deployment may exist
	// on the same (app, environment, branch).
	//
	// The new deployment is persisted with trigger=UNKEY and the provided
	// reason so this path is visible in the deployments list.
	RebuildDeployment(context.Context, *connect.Request[v1.RebuildDeploymentRequest]) (*connect.Response[v1.RebuildDeploymentResponse], error)
}

// NewOpsServiceClient constructs a client for the ctrl.v1.OpsService service. By default, it uses
// the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewOpsServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) OpsServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	opsServiceMethods := v1.File_ctrl_v1_ops_proto.Services().ByName("OpsService").Methods()
	return &opsServiceClient{
		rebuildDeployment: connect.NewClient[v1.RebuildDeploymentRequest, v1.RebuildDeploymentResponse](
			httpClient,
			baseURL+OpsServiceRebuildDeploymentProcedure,
			connect.WithSchema(opsServiceMethods.ByName("RebuildDeployment")),
			connect.WithClientOptions(opts...),
		),
	}
}

// opsServiceClient implements OpsServiceClient.
type opsServiceClient struct {
	rebuildDeployment *connect.Client[v1.RebuildDeploymentRequest, v1.RebuildDeploymentResponse]
}

// RebuildDeployment calls ctrl.v1.OpsService.RebuildDeployment.
func (c *opsServiceClient) RebuildDeployment(ctx context.Context, req *connect.Request[v1.RebuildDeploymentRequest]) (*connect.Response[v1.RebuildDeploymentResponse], error) {
	return c.rebuildDeployment.CallUnary(ctx, req)
}

// OpsServiceHandler is an implementation of the ctrl.v1.OpsService service.
type OpsServiceHandler interface {
	// RebuildDeployment creates a new deployment that clones the given source
	// deployment's project, app, and environment, then kicks off the deploy
	// workflow. The new deployment inherits the app's *current* runtime
	// settings and env vars — config drift since the source applies.
	//
	// Source resolution:
	//  1. If the source has a git_commit_sha AND the app has a github repo
	//     connection, rebuild from the pinned SHA.
	//  2. Otherwise reuse the source deployment's docker image verbatim.
	//  3. If neither is available, the RPC fails.
	//
	// Guardrail (unless `force` is true): no newer active deployment may exist
	// on the same (app, environment, branch).
	//
	// The new deployment is persisted with trigger=UNKEY and the provided
	// reason so this path is visible in the deployments list.
	RebuildDeployment(context.Context, *connect.Request[v1.RebuildDeploymentRequest]) (*connect.Response[v1.RebuildDeploymentResponse], error)
}

// NewOpsServiceHandler builds an HTTP handler from the service implementation. It returns the path
// on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewOpsServiceHandler(svc OpsServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	opsServiceMethods := v1.File_ctrl_v1_ops_proto.Services().ByName("OpsService").Methods()
	opsServiceRebuildDeploymentHandler := connect.NewUnaryHandler(
		OpsServiceRebuildDeploymentProcedure,
		svc.RebuildDeployment,
		connect.WithSchema(opsServiceMethods.ByName("RebuildDeployment")),
		connect.WithHandlerOptions(opts...),
	)
	return "/ctrl.v1.OpsService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case OpsServiceRebuildDeploymentProcedure:
			opsServiceRebuildDeploymentHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedOpsServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedOpsServiceHandler struct{}

func (UnimplementedOpsServiceHandler) RebuildDeployment(context.Context, *connect.Request[v1.RebuildDeploymentRequest]) (*connect.Response[v1.RebuildDeploymentResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("ctrl.v1.OpsService.RebuildDeployment is not implemented"))
}
