/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type V2PortalGetVerificationsDataPoint = {
  /**
   * Bucket start as a unix timestamp in milliseconds.
   */
  time: number;
  /**
   * Total verifications in this bucket, across all outcomes.
   */
  total: number;
  /**
   * Verifications with a VALID outcome.
   */
  valid: number;
  /**
   * Verifications rejected because a rate limit was exceeded.
   */
  rateLimited: number;
  /**
   * Verifications rejected for insufficient permissions.
   */
  insufficientPermissions: number;
  /**
   * Verifications rejected as forbidden.
   */
  forbidden: number;
  /**
   * Verifications rejected because the key was disabled.
   */
  disabled: number;
  /**
   * Verifications rejected because the key was expired.
   */
  expired: number;
  /**
   * Verifications rejected because remaining usage was exhausted.
   */
  usageExceeded: number;
};

/** @internal */
export const V2PortalGetVerificationsDataPoint$inboundSchema: z.ZodType<
  V2PortalGetVerificationsDataPoint,
  z.ZodTypeDef,
  unknown
> = z.object({
  time: z.number().int(),
  total: z.number().int(),
  valid: z.number().int(),
  rateLimited: z.number().int(),
  insufficientPermissions: z.number().int(),
  forbidden: z.number().int(),
  disabled: z.number().int(),
  expired: z.number().int(),
  usageExceeded: z.number().int(),
});

export function v2PortalGetVerificationsDataPointFromJSON(
  jsonString: string,
): SafeParseResult<V2PortalGetVerificationsDataPoint, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => V2PortalGetVerificationsDataPoint$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'V2PortalGetVerificationsDataPoint' from JSON`,
  );
}
