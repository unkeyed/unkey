/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * A rate limit applied during key verification. `limit` and `duration`
 *
 * @remarks
 * must be set together or both omitted; a partial pair is rejected.
 */
export type KeyRatelimit = {
  /**
   * Name of a rate limit configured on the key or its identity, or the name
   *
   * @remarks
   * of the inline override defined by `limit` and `duration`.
   */
  name: string;
  /**
   * Inline override: maximum number of operations per window. Must be set
   *
   * @remarks
   * together with `duration`.
   */
  limit?: number | undefined;
  /**
   * Inline override: window duration in milliseconds. Must be set together
   *
   * @remarks
   * with `limit`.
   */
  duration?: number | undefined;
  /**
   * Cost charged against the limit per request. Defaults to 1.
   */
  cost?: number | undefined;
};

/** @internal */
export const KeyRatelimit$inboundSchema: z.ZodType<
  KeyRatelimit,
  z.ZodTypeDef,
  unknown
> = z.object({
  name: z.string(),
  limit: z.number().int().optional(),
  duration: z.number().int().optional(),
  cost: z.number().int().optional(),
});
/** @internal */
export type KeyRatelimit$Outbound = {
  name: string;
  limit?: number | undefined;
  duration?: number | undefined;
  cost?: number | undefined;
};

/** @internal */
export const KeyRatelimit$outboundSchema: z.ZodType<
  KeyRatelimit$Outbound,
  z.ZodTypeDef,
  KeyRatelimit
> = z.object({
  name: z.string(),
  limit: z.number().int().optional(),
  duration: z.number().int().optional(),
  cost: z.number().int().optional(),
});

export function keyRatelimitToJSON(keyRatelimit: KeyRatelimit): string {
  return JSON.stringify(KeyRatelimit$outboundSchema.parse(keyRatelimit));
}
export function keyRatelimitFromJSON(
  jsonString: string,
): SafeParseResult<KeyRatelimit, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => KeyRatelimit$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'KeyRatelimit' from JSON`,
  );
}
