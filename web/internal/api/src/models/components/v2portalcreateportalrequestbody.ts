/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import {
  PortalMapping,
  PortalMapping$Outbound,
  PortalMapping$outboundSchema,
} from "./portalmapping.js";

export type V2PortalCreatePortalRequestBody = {
  /**
   * URL-safe handle for this portal, unique within your workspace. Lowercase
   *
   * @remarks
   * alphanumerics and single hyphens, 3-64 characters, not starting or ending
   * with a hyphen.
   */
  slug: string;
  /**
   * Human-readable name your end users see in the portal header and page
   *
   * @remarks
   * titles. Unlike `slug` it is not part of any URL, so it can be changed
   * freely.
   */
  displayName: string;
  /**
   * The single resource a portal serves keys for.
   *
   * @remarks
   *
   * A portal maps to exactly one app or one keyspace, never several and never
   * neither. Naming the kind and the id together makes that invariant part of the
   * request shape: there is no way to express a portal with two mappings, and no
   * way to express one with none.
   */
  mapping: PortalMapping;
  /**
   * Whether sessions can be minted for this portal immediately. Defaults to
   *
   * @remarks
   * true; send false to create it dormant and enable it later.
   */
  enabled?: boolean | undefined;
  /**
   * Absolute `https://` URL of the logo shown in the portal header.
   *
   * @remarks
   *
   * Loaded by your end users' browsers, so the host you name receives their IP
   * and user agent on every page view.
   */
  logoUrl?: string | undefined;
  /**
   * Six-digit hex colour used for primary actions and accents in the portal.
   *
   * @remarks
   */
  primaryColor?: string | undefined;
};

/** @internal */
export type V2PortalCreatePortalRequestBody$Outbound = {
  slug: string;
  displayName: string;
  mapping: PortalMapping$Outbound;
  enabled: boolean;
  logoUrl?: string | undefined;
  primaryColor?: string | undefined;
};

/** @internal */
export const V2PortalCreatePortalRequestBody$outboundSchema: z.ZodType<
  V2PortalCreatePortalRequestBody$Outbound,
  z.ZodTypeDef,
  V2PortalCreatePortalRequestBody
> = z.object({
  slug: z.string(),
  displayName: z.string(),
  mapping: PortalMapping$outboundSchema,
  enabled: z.boolean().default(true),
  logoUrl: z.string().optional(),
  primaryColor: z.string().optional(),
});

export function v2PortalCreatePortalRequestBodyToJSON(
  v2PortalCreatePortalRequestBody: V2PortalCreatePortalRequestBody,
): string {
  return JSON.stringify(
    V2PortalCreatePortalRequestBody$outboundSchema.parse(
      v2PortalCreatePortalRequestBody,
    ),
  );
}
