/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { ClosedEnum } from "../../types/enums.js";

/**
 * How the value may be read back. `writeonly` values can never be read back
 *
 * @remarks
 * through the API; `recoverable` values can be decrypted. Values are encrypted
 * at rest either way.
 */
export const EnvironmentVariableKind = {
  Recoverable: "recoverable",
  Writeonly: "writeonly",
} as const;
/**
 * How the value may be read back. `writeonly` values can never be read back
 *
 * @remarks
 * through the API; `recoverable` values can be decrypted. Values are encrypted
 * at rest either way.
 */
export type EnvironmentVariableKind = ClosedEnum<
  typeof EnvironmentVariableKind
>;

/** @internal */
export const EnvironmentVariableKind$inboundSchema: z.ZodNativeEnum<
  typeof EnvironmentVariableKind
> = z.nativeEnum(EnvironmentVariableKind);
