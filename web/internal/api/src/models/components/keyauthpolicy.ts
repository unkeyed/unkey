/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  KeyLocation,
  KeyLocation$inboundSchema,
  KeyLocation$Outbound,
  KeyLocation$outboundSchema,
} from "./keylocation.js";
import {
  KeyRatelimit,
  KeyRatelimit$inboundSchema,
  KeyRatelimit$Outbound,
  KeyRatelimit$outboundSchema,
} from "./keyratelimit.js";

/**
 * Verifies Unkey API keys on matching requests.
 */
export type KeyauthPolicy = {
  /**
   * Keyspaces to verify keys against, referenced by id. All keyspaces must
   *
   * @remarks
   * belong to your workspace.
   */
  keyspaces: Array<string>;
  /**
   * Where to look for the key on incoming requests, tried in order. Defaults
   *
   * @remarks
   * to the `Authorization Bearer` header when omitted.
   */
  locations?: Array<KeyLocation> | undefined;
  /**
   * Optional permission query the verified key must satisfy, e.g.
   *
   * @remarks
   * `documents.read AND documents.write`.
   */
  permissionQuery?: string | undefined;
  /**
   * Rate limits applied during key verification.
   */
  ratelimits?: Array<KeyRatelimit> | undefined;
};

/** @internal */
export const KeyauthPolicy$inboundSchema: z.ZodType<
  KeyauthPolicy,
  z.ZodTypeDef,
  unknown
> = z.object({
  keyspaces: z.array(z.string()),
  locations: z.array(KeyLocation$inboundSchema).optional(),
  permissionQuery: z.string().optional(),
  ratelimits: z.array(KeyRatelimit$inboundSchema).optional(),
});
/** @internal */
export type KeyauthPolicy$Outbound = {
  keyspaces: Array<string>;
  locations?: Array<KeyLocation$Outbound> | undefined;
  permissionQuery?: string | undefined;
  ratelimits?: Array<KeyRatelimit$Outbound> | undefined;
};

/** @internal */
export const KeyauthPolicy$outboundSchema: z.ZodType<
  KeyauthPolicy$Outbound,
  z.ZodTypeDef,
  KeyauthPolicy
> = z.object({
  keyspaces: z.array(z.string()),
  locations: z.array(KeyLocation$outboundSchema).optional(),
  permissionQuery: z.string().optional(),
  ratelimits: z.array(KeyRatelimit$outboundSchema).optional(),
});

export function keyauthPolicyToJSON(keyauthPolicy: KeyauthPolicy): string {
  return JSON.stringify(KeyauthPolicy$outboundSchema.parse(keyauthPolicy));
}
export function keyauthPolicyFromJSON(
  jsonString: string,
): SafeParseResult<KeyauthPolicy, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => KeyauthPolicy$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'KeyauthPolicy' from JSON`,
  );
}
