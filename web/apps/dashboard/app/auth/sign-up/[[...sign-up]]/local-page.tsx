import { LocalAuthLanding } from "../../local-auth-landing";

export function LocalSignUpPage() {
  return (
    <LocalAuthLanding description="Account creation is disabled in local mode. Continue with the built-in local account." />
  );
}
