// Moved to the prototype folder: the projects list card shows each project's
// latest deployment too, so the generator can't live inside one page's folder.
export * from "@/app/(app)/[workspaceSlug]/projects/_components/prototype/deployments-mock";
