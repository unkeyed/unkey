"use client";

import { AppSidebar } from "@/components/navigation/sidebar/app-sidebar";

/**
 * The `current` variant is the production sidebar, untouched.
 * Re-exported here so the dispatcher has a uniform API across variants.
 */
export const CurrentVariant = AppSidebar;
