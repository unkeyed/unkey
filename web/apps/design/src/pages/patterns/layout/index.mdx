---
layout: ../../../layouts/layout.astro
title: Layout — Unkey Design
---
import { Preview } from "../../../components/preview.tsx";
import { BasicExample } from "./_basic.tsx";
import { SecondaryNavExample } from "./_secondary-nav.tsx";
import { FullWidthExample } from "./_full-width.tsx";
import basicSource from "./_basic.tsx?highlight";
import secondaryNavSource from "./_secondary-nav.tsx?highlight";
import fullWidthSource from "./_full-width.tsx?highlight";

# Layout

Rules and guidelines for building effective page layouts on the Unkey dashboard.

## Basics


A simple page with a header and actions.


<Preview client:load bleed code={basicSource}>
  <BasicExample />
</Preview>

## Secondary navigation

A `SecondaryNav` rail on the left, the page on the right. Use it for sections with sibling pages.

<Preview client:load bleed code={secondaryNavSource}>
  <SecondaryNavExample />
</Preview>

Add the rail in the section layout, then let each page render its own `PageBody`.

## Full width

Logs, tables, and editors need the whole viewport, so the body runs edge to edge. The header keeps a gutter and a divider, so its title and actions still line up with the rest of the app.

<Preview client:load bleed code={fullWidthSource}>
  <FullWidthExample />
</Preview>
