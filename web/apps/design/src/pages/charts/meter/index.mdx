---
layout: ../../../layouts/layout.astro
title: Meter — Unkey Design
---
import { Preview } from "../../../components/preview.tsx";
import { BasicMeter } from "./_basic.tsx";
import basicSource from "./_basic.tsx?highlight";

# Meter

A basic meter component.

<Preview client:load code={basicSource}>
  <BasicMeter />
</Preview>

## Usage

Use a meter to show how much of a resource has been used. It works best against
a fixed limit, like a monthly request quota or a workspace CPU ceiling.

