version: "2"
sql:
  - engine: "sqlite"
    queries: "queries.sql"
    schema: "schema.sql"
    gen:
      go:
        package: "database"
        out: "../internal/database"
        emit_db_tags: true
        emit_json_tags: true
        emit_interface: true
