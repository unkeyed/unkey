// Package routing provides services for gateway configuration lookup and routing.
// It handles the resolution of incoming requests to appropriate backend targets
// based on configurable routing rules stored in the database.
package routing
