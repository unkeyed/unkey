Response:
  description: |-
    Successfully created a new identity. The identity can now be associated with API keys to enable resource sharing and consistent rate limiting.
  content:
    application/json:
      schema:
        $ref: "#/V2IdentitiesCreateIdentityResponseBody"
      examples:
        success:
          summary: Successfully created identity
          value:
            meta:
              requestId: req_01H9TQPP77V5E48E9SH0BG0ZQX

V2IdentitiesCreateIdentityResponseBody:
  type: object
  required:
    - meta
    - data
  properties:
    meta:
      $ref: "../../../../common/meta.yaml"
    data:
      "$ref": "#/IdentitiesCreateIdentityResponseData"
  additionalProperties: false

IdentitiesCreateIdentityResponseData:
  type: object
  additionalProperties: false