Response:
  description: Successfully retrieved key information
  content:
    application/json:
      schema:
        "$ref": "#/V2KeysGetKeyResponseBody"

V2KeysGetKeyResponseBody:
  type: object
  required:
    - meta
    - data
  properties:
    meta:
      $ref: "../../../../common/Meta.yaml"
    data:
      "$ref": "../../../../common/KeyResponseData.yaml"
  additionalProperties: false
