// Package batch provides utilities for efficiently processing items in batches.
// It offers mechanisms to collect items until a batch size threshold is reached
// or a time interval has elapsed, then flushes them as a group.
package batch
