// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.27.0
// source: get.sql

package mysql

import (
	"context"
	"database/sql"
)

const get = `-- name: Get :one
SELECT id, workspace_id, ` + "`" + `key` + "`" + `, value, ttl, created_at FROM kv
WHERE ` + "`" + `key` + "`" + ` = ? AND (ttl IS NULL OR ttl > ?)
`

type GetParams struct {
	Key string
	Ttl sql.NullInt64
}

func (q *Queries) Get(ctx context.Context, arg GetParams) (Kv, error) {
	row := q.db.QueryRowContext(ctx, get, arg.Key, arg.Ttl)
	var i Kv
	err := row.Scan(
		&i.ID,
		&i.WorkspaceID,
		&i.Key,
		&i.Value,
		&i.Ttl,
		&i.CreatedAt,
	)
	return i, err
}
