// Code generated by sqlc bulk insert plugin. DO NOT EDIT.

package db

import (
	"context"
	"fmt"
	"strings"
)

// bulkUpsertInstance is the base query for bulk insert
const bulkUpsertInstance = `INSERT INTO instance (id, deployment_id, status, config) VALUES %s ON DUPLICATE KEY UPDATE
  deployment_id = VALUES(deployment_id),
  status = VALUES(status),
  config = VALUES(config)`

// UpsertInstance performs bulk insert in a single query
func (q *BulkQueries) UpsertInstance(ctx context.Context, db DBTX, args []UpsertInstanceParams) error {

	if len(args) == 0 {
		return nil
	}

	// Build the bulk insert query
	valueClauses := make([]string, len(args))
	for i := range args {
		valueClauses[i] = "(?, ?, ?, ?)"
	}

	bulkQuery := fmt.Sprintf(bulkUpsertInstance, strings.Join(valueClauses, ", "))

	// Collect all arguments
	var allArgs []any
	for _, arg := range args {
		allArgs = append(allArgs, arg.ID)
		allArgs = append(allArgs, arg.DeploymentID)
		allArgs = append(allArgs, arg.Status)
		allArgs = append(allArgs, arg.Config)
	}

	// Execute the bulk insert
	_, err := db.ExecContext(ctx, bulkQuery, allArgs...)
	return err
}
