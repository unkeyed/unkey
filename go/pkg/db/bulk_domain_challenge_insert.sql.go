// Code generated by sqlc bulk insert plugin. DO NOT EDIT.

package db

import (
	"context"
	"fmt"
	"strings"
)

// bulkInsertDomainChallenge is the base query for bulk insert
const bulkInsertDomainChallenge = `INSERT INTO domain_challenges ( workspace_id, domain_id, token, authorization, status, created_at, updated_at, expires_at ) VALUES %s`

// InsertDomainChallenges performs bulk insert in a single query
func (q *BulkQueries) InsertDomainChallenges(ctx context.Context, db DBTX, args []InsertDomainChallengeParams) error {

	if len(args) == 0 {
		return nil
	}

	// Build the bulk insert query
	valueClauses := make([]string, len(args))
	for i := range args {
		valueClauses[i] = "( ?, ?, ?, ?, ?, ?, ?, ? )"
	}

	bulkQuery := fmt.Sprintf(bulkInsertDomainChallenge, strings.Join(valueClauses, ", "))

	// Collect all arguments
	var allArgs []any
	for _, arg := range args {
		allArgs = append(allArgs, arg.WorkspaceID)
		allArgs = append(allArgs, arg.DomainID)
		allArgs = append(allArgs, arg.Token)
		allArgs = append(allArgs, arg.Authorization)
		allArgs = append(allArgs, arg.Status)
		allArgs = append(allArgs, arg.CreatedAt)
		allArgs = append(allArgs, arg.UpdatedAt)
		allArgs = append(allArgs, arg.ExpiresAt)
	}

	// Execute the bulk insert
	_, err := db.ExecContext(ctx, bulkQuery, allArgs...)
	return err
}
