# bun server with unkey

A simple bun server with unkey.

## Install

```bash
bun install
```

## Run

```bash
bun run dev
```

## Test

```bash
curl http://localhost:8000 -H "Authorization: Bearer <KEY>"
```
