name: Unit Tests
on:
  workflow_call:

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4


      - name: Install
        uses: ./.github/actions/install
        with:
          ts: true

      - name: Build
        uses: ./.github/actions/build

      - name: Test
        run: pnpm test
