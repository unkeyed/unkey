#!/usr/bin/env bash
#MISE description="Format the repository"
#MISE depends=["install-web"]
set -euo pipefail

# format yaml
dprint fmt --config dev/dprint.json

# format go
go fmt ./...

# format protos
go tool buf format -w

# format ts
pnpm --dir=web fmt

# format mise config
mise fmt
