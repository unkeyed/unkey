// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: blocklist_list_active.sql

package db

import (
	"context"
)

const blocklistListActive = `-- name: BlocklistListActive :many
SELECT
    workspace_id,
    namespace,
    identifier,
    duration_ms,
    sequence,
    ` + "`" + `limit` + "`" + `,
    expires_at
FROM ratelimit_blocklist
WHERE expires_at > ?
`

type BlocklistListActiveRow struct {
	WorkspaceID string `db:"workspace_id"`
	Namespace   string `db:"namespace"`
	Identifier  string `db:"identifier"`
	DurationMs  uint64 `db:"duration_ms"`
	Sequence    int64  `db:"sequence"`
	Limit       uint64 `db:"limit"`
	ExpiresAt   uint64 `db:"expires_at"`
}

// BlocklistListActive returns every still-active denial for the sync loop to
// apply locally. The result set is bounded by unique violators currently in
// their TTL window; the expires_at index keeps the scan cheap.
//
//	SELECT
//	    workspace_id,
//	    namespace,
//	    identifier,
//	    duration_ms,
//	    sequence,
//	    `limit`,
//	    expires_at
//	FROM ratelimit_blocklist
//	WHERE expires_at > ?
func (q *Queries) BlocklistListActive(ctx context.Context, now uint64) ([]BlocklistListActiveRow, error) {
	rows, err := q.db.QueryContext(ctx, blocklistListActive, now)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []BlocklistListActiveRow
	for rows.Next() {
		var i BlocklistListActiveRow
		if err := rows.Scan(
			&i.WorkspaceID,
			&i.Namespace,
			&i.Identifier,
			&i.DurationMs,
			&i.Sequence,
			&i.Limit,
			&i.ExpiresAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
