// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: window_counts_delete_expired.sql

package db

import (
	"context"
)

const windowCountsDeleteExpired = `-- name: WindowCountsDeleteExpired :execrows
DELETE FROM ratelimit_window_counts
WHERE expires_at < ?
`

// WindowCountsDeleteExpired removes rows whose grace period has passed and
// returns the number of rows deleted so the caller can surface it for
// observability. Called by an external Restate cron, not the ratelimit
// service itself.
//
//	DELETE FROM ratelimit_window_counts
//	WHERE expires_at < ?
func (q *Queries) WindowCountsDeleteExpired(ctx context.Context, cutoff uint64) (int64, error) {
	result, err := q.db.ExecContext(ctx, windowCountsDeleteExpired, cutoff)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected()
}
