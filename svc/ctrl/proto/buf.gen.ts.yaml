version: v2
managed:
  enabled: true
plugins:
  # Generate TypeScript for ctrl protos (dashboard API)
  - remote: buf.build/bufbuild/es:v2.8.0
    out: ../../../web/apps/dashboard/gen/proto
    opt:
      - target=ts
