// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: alert_event_list_open_groups.sql

package db

import (
	"context"
)

const listOpenAlertEventGroups = `-- name: ListOpenAlertEventGroups :many
SELECT DISTINCT
    workspace_id,
    project_id,
    app_id,
    environment_id
FROM alert_events
WHERE status = 'open'
  AND workspace_hash % ? = ?
ORDER BY workspace_id, project_id, app_id, environment_id
`

type ListOpenAlertEventGroupsParams struct {
	ShardCount uint64 `db:"shard_count"`
	Shard      uint64 `db:"shard"`
}

type ListOpenAlertEventGroupsRow struct {
	WorkspaceID   string `db:"workspace_id"`
	ProjectID     string `db:"project_id"`
	AppID         string `db:"app_id"`
	EnvironmentID string `db:"environment_id"`
}

// ListOpenAlertEventGroups returns the durable groups that shards must keep
// evaluating even when ClickHouse no longer classifies them as candidates.
// workspace_hash uses the same CityHash64 function as ClickHouse sharding.
//
//	SELECT DISTINCT
//	    workspace_id,
//	    project_id,
//	    app_id,
//	    environment_id
//	FROM alert_events
//	WHERE status = 'open'
//	  AND workspace_hash % ? = ?
//	ORDER BY workspace_id, project_id, app_id, environment_id
func (q *Queries) ListOpenAlertEventGroups(ctx context.Context, arg ListOpenAlertEventGroupsParams) ([]ListOpenAlertEventGroupsRow, error) {
	rows, err := q.db.QueryContext(ctx, listOpenAlertEventGroups, arg.ShardCount, arg.Shard)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListOpenAlertEventGroupsRow
	for rows.Next() {
		var i ListOpenAlertEventGroupsRow
		if err := rows.Scan(
			&i.WorkspaceID,
			&i.ProjectID,
			&i.AppID,
			&i.EnvironmentID,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
