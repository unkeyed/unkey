// Code generated by sqlc bulk insert plugin. DO NOT EDIT.

package db

import (
	"context"
	"fmt"
	"strings"
)

// bulkUpsertAppDockerSource is the base query for bulk insert
const bulkUpsertAppDockerSource = `INSERT INTO app_docker_sources ( workspace_id, app_id, image, created_at, updated_at ) VALUES %s ON DUPLICATE KEY UPDATE
    image = VALUES(image),
    updated_at = VALUES(updated_at)`

// UpsertAppDockerSource performs bulk insert in a single query

func (q *BulkQueries) UpsertAppDockerSource(ctx context.Context, args []UpsertAppDockerSourceParams) error {

	if len(args) == 0 {
		return nil
	}

	// Build the bulk insert query
	valueClauses := make([]string, len(args))
	for i := range args {
		valueClauses[i] = "( ?, ?, ?, ?, ? )"
	}

	bulkQuery := fmt.Sprintf(bulkUpsertAppDockerSource, strings.Join(valueClauses, ", "))

	// Collect all arguments
	var allArgs []any
	for _, arg := range args {
		allArgs = append(allArgs, arg.WorkspaceID)
		allArgs = append(allArgs, arg.AppID)
		allArgs = append(allArgs, arg.Image)
		allArgs = append(allArgs, arg.CreatedAt)
		allArgs = append(allArgs, arg.UpdatedAt)
	}

	// Execute the bulk insert
	_, err := q.db.ExecContext(ctx, bulkQuery, allArgs...)
	return err
}
