// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: deployment_filter_existing_ids.sql

package db

import (
	"context"
	"strings"
)

const filterExistingDeploymentIds = `-- name: FilterExistingDeploymentIds :many
SELECT id FROM deployments
WHERE id IN (/*SLICE:ids*/?)
`

// FilterExistingDeploymentIds returns the subset of the given deployment ids
// that exist. Used by the registry sweep to decide which Depot image tags
// are orphaned.
//
//	SELECT id FROM deployments
//	WHERE id IN (/*SLICE:ids*/?)
func (q *Queries) FilterExistingDeploymentIds(ctx context.Context, ids []string) ([]string, error) {
	query := filterExistingDeploymentIds
	var queryParams []interface{}
	if len(ids) > 0 {
		for _, v := range ids {
			queryParams = append(queryParams, v)
		}
		query = strings.Replace(query, "/*SLICE:ids*/?", strings.Repeat(",?", len(ids))[1:], 1)
	} else {
		query = strings.Replace(query, "/*SLICE:ids*/?", "NULL", 1)
	}
	rows, err := q.db.QueryContext(ctx, query, queryParams...)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []string
	for rows.Next() {
		var id string
		if err := rows.Scan(&id); err != nil {
			return nil, err
		}
		items = append(items, id)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
