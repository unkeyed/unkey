// Code generated by sqlc bulk insert plugin. DO NOT EDIT.

package db

import (
	"context"
	"fmt"
	"strings"
)

// bulkInsertDeployAnomalyEvent is the base query for bulk insert
const bulkInsertDeployAnomalyEvent = `INSERT INTO deploy_anomaly_events ( id, workspace_id, project_id, app_id, environment_id, deployment_id, metric, event_time, received_at ) VALUES %s ON DUPLICATE KEY UPDATE id = id`

// InsertDeployAnomalyEvents performs bulk insert in a single query

func (q *BulkQueries) InsertDeployAnomalyEvents(ctx context.Context, args []InsertDeployAnomalyEventParams) error {

	if len(args) == 0 {
		return nil
	}

	// Build the bulk insert query
	valueClauses := make([]string, len(args))
	for i := range args {
		valueClauses[i] = "(?, ?, ?, ?, ?, ?, ?, ?, ?)"
	}

	bulkQuery := fmt.Sprintf(bulkInsertDeployAnomalyEvent, strings.Join(valueClauses, ", "))

	// Collect all arguments
	var allArgs []any
	for _, arg := range args {
		allArgs = append(allArgs, arg.ID)
		allArgs = append(allArgs, arg.WorkspaceID)
		allArgs = append(allArgs, arg.ProjectID)
		allArgs = append(allArgs, arg.AppID)
		allArgs = append(allArgs, arg.EnvironmentID)
		allArgs = append(allArgs, arg.DeploymentID)
		allArgs = append(allArgs, arg.Metric)
		allArgs = append(allArgs, arg.EventTime)
		allArgs = append(allArgs, arg.ReceivedAt)
	}

	// Execute the bulk insert
	_, err := q.db.ExecContext(ctx, bulkQuery, allArgs...)
	return err
}
