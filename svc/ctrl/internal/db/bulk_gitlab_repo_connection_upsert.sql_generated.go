// Code generated by sqlc bulk insert plugin. DO NOT EDIT.

package db

import (
	"context"
	"fmt"
	"strings"
)

// bulkUpsertGitlabRepoConnection is the base query for bulk insert
const bulkUpsertGitlabRepoConnection = `INSERT INTO github_repo_connections ( workspace_id, project_id, app_id, installation_id, repository_id, repository_full_name, provider, access_token, created_at ) VALUES %s ON DUPLICATE KEY UPDATE
    installation_id = VALUES(installation_id),
    repository_id = VALUES(repository_id),
    repository_full_name = VALUES(repository_full_name),
    provider = VALUES(provider),
    access_token = VALUES(access_token),
    updated_at = VALUES(created_at)`

// UpsertGitlabRepoConnection performs bulk insert in a single query

func (q *BulkQueries) UpsertGitlabRepoConnection(ctx context.Context, args []UpsertGitlabRepoConnectionParams) error {

	if len(args) == 0 {
		return nil
	}

	// Build the bulk insert query
	valueClauses := make([]string, len(args))
	for i := range args {
		valueClauses[i] = "( ?, ?, ?, ?, ?, ?, 'gitlab', ?, ? )"
	}

	bulkQuery := fmt.Sprintf(bulkUpsertGitlabRepoConnection, strings.Join(valueClauses, ", "))

	// Collect all arguments
	var allArgs []any
	for _, arg := range args {
		allArgs = append(allArgs, arg.WorkspaceID)
		allArgs = append(allArgs, arg.ProjectID)
		allArgs = append(allArgs, arg.AppID)
		allArgs = append(allArgs, arg.InstallationID)
		allArgs = append(allArgs, arg.RepositoryID)
		allArgs = append(allArgs, arg.RepositoryFullName)
		allArgs = append(allArgs, arg.AccessToken)
		allArgs = append(allArgs, arg.CreatedAt)
	}

	// Execute the bulk insert
	_, err := q.db.ExecContext(ctx, bulkQuery, allArgs...)
	return err
}
