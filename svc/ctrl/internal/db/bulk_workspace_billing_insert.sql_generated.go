// Code generated by sqlc bulk insert plugin. DO NOT EDIT.

package db

import (
	"context"
	"fmt"
	"strings"
)

// bulkInsertWorkspaceBilling is the base query for bulk insert
const bulkInsertWorkspaceBilling = `INSERT INTO ` + "`" + `workspace_billing` + "`" + ` ( workspace_id, tier, created_at_m ) VALUES %s ON DUPLICATE KEY UPDATE workspace_id = workspace_id`

// InsertWorkspaceBillings performs bulk insert in a single query

func (q *BulkQueries) InsertWorkspaceBillings(ctx context.Context, args []InsertWorkspaceBillingParams) error {

	if len(args) == 0 {
		return nil
	}

	// Build the bulk insert query
	valueClauses := make([]string, len(args))
	for i := range args {
		valueClauses[i] = "( ?, 'Free', ? )"
	}

	bulkQuery := fmt.Sprintf(bulkInsertWorkspaceBilling, strings.Join(valueClauses, ", "))

	// Collect all arguments
	var allArgs []any
	for _, arg := range args {
		allArgs = append(allArgs, arg.WorkspaceID)
		allArgs = append(allArgs, arg.CreatedAt)
	}

	// Execute the bulk insert
	_, err := q.db.ExecContext(ctx, bulkQuery, allArgs...)
	return err
}
