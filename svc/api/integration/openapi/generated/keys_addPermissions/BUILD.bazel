load("@rules_go//go:def.bzl", "go_test")

go_test(
    name = "keys_addPermissions_test",
    srcs = ["generated_test.go"],
    data = [
        "//svc/api/cmd:api",  # API binary for subprocess execution
        "//svc/api/routes/v2_keys_add_permissions",  # keep - Route handler dependency for Bazel invalidation
    ],
    tags = ["docker"],
    deps = ["//svc/api/integration/openapi"],
)
