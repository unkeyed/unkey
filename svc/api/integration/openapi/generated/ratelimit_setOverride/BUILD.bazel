load("@rules_go//go:def.bzl", "go_test")

go_test(
    name = "ratelimit_setOverride_test",
    srcs = ["generated_test.go"],
    data = [
        "//:unkey",  # Unkey binary for subprocess execution (runs: unkey run api)
        "//svc/api/routes/v2_ratelimit_set_override",  # keep - Route handler dependency for Bazel invalidation
    ],
    tags = ["docker"],
    deps = ["//svc/api/integration/openapi"],
)
