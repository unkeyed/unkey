type: string
enum:
  - running
  - stopped
x-enum-varnames:
  - DeploymentDesiredStateRunning
  - DeploymentDesiredStateStopped
description: |
  The intended state of the deployment, independent of its current lifecycle
  status. `running` means it should serve traffic; `stopped` means it has been
  intentionally taken down.
example: running
