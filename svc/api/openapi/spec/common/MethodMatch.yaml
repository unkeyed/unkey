type: object
required:
  - methods
properties:
  methods:
    type: array
    minItems: 1
    items:
      type: string
      enum:
        - GET
        - POST
        - PUT
        - PATCH
        - DELETE
        - HEAD
        - OPTIONS
additionalProperties: false
description: Matches when the request method is one of the listed methods.
