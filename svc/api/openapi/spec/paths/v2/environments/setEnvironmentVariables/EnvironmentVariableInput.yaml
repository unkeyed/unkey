type: object
description: A single environment variable to set.
required:
  - key
  - value
properties:
  key:
    type: string
    minLength: 1
    maxLength: 256
    pattern: "^[A-Za-z_][A-Za-z0-9_]*$"
    description: |
      The variable name. Must be a POSIX shell name: letters, digits, and
      underscores only, and must not start with a digit. Other names are
      unreachable from shells and most runtimes.
    example: DATABASE_URL
  value:
    type: string
    minLength: 1
    maxLength: 4096
    description: The variable value. Always encrypted at rest.
    example: postgresql://user:pass@host:5432/db
  kind:
    type: string
    enum:
      - recoverable
      - writeonly
    default: writeonly
    description: |
      How the value may be read back. `writeonly` values can never be read
      back through the API; `recoverable` values can be decrypted. Values are
      encrypted at rest either way. Defaults to `writeonly`.
    example: writeonly
  description:
    type: string
    maxLength: 255
    description: |
      Human-readable description of the variable.
    example: Primary database connection string
additionalProperties: false
