type: object
required:
  - project
  - app
  - environment
properties:
  project:
    "$ref": "../../../../common/ResourceIdentifier.yaml"
  app:
    "$ref": "../../../../common/ResourceIdentifier.yaml"
  environment:
    "$ref": "../../../../common/ResourceIdentifier.yaml"
  git:
    "$ref": "./DeploymentSourceGit.yaml"
  docker:
    "$ref": "./DeploymentSourceDocker.yaml"
  deployment:
    "$ref": "./DeploymentSourceDeployment.yaml"
additionalProperties: false
oneOf:
  - required: [docker]
  - required: [git]
  - required: [deployment]
description: Create a deployment. Provide exactly one of git, docker, or deployment.
