post:
  tags:
    - domains
  summary: List domains
  description: |
    List custom domains in your workspace and their verification status. Use the optional
    project, app, and environment filters to narrow the results.

    Results are paginated and sorted by their ID. When `hasMore` is true, send the
    returned `cursor` to get the next page. A workspace or filtered resource with no
    domains returns an empty array, not a 404.

    `status: verified` means the domain is verified. Unkey has configured routing and requested a
    certificate. Each domain includes its full `dnsRecords`. Each record has a `verified` flag.
    The flag shows which records Unkey has read back, so you can see which records are still
    missing without a second call. Some providers hide a record from DNS lookups, for example a
    proxied or flattened routing record. Such a record stays `false` while it serves traffic.

    **Required Permissions**

    The endpoint accepts a canonical `#read` permission that matches the requested domain
    collection.

    The endpoint also accepts the legacy `environment.*.read_domain` permission. An
    environment list accepts `environment.<environment_id>.read_domain` too.
  operationId: domains.listDomains
  x-speakeasy-name-override: listDomains
  x-unkey-idempotency: idempotent
  security:
    - bearer: []
  requestBody:
    content:
      application/json:
        schema:
          "$ref": "./V2DomainsListDomainsRequestBody.yaml"
        examples:
          workspaceWide:
            summary: List all domains in the workspace
            value: {}
          byProject:
            summary: List a project's domains
            value:
              project: payments
          bySlugs:
            summary: List an environment's domains by slugs
            description: Uses the project, app, and environment slugs.
            value:
              project: payments
              app: payments-api
              environment: production
          byIds:
            summary: List an environment's domains by its ID
            value:
              environment: env_1234abcd
          search:
            summary: Filter by name
            description: Returns only domains whose id or name contains the search string.
            value:
              project: payments
              app: payments-api
              environment: production
              search: acme.com
          paginated:
            summary: Paginate with a smaller page size
            description: Get a smaller page and continue from a cursor.
            value:
              project: payments
              app: payments-api
              environment: production
              limit: 20
              cursor: dom_1234abcd
    required: true
  responses:
    "200":
      description: |
        Successfully retrieved the domains that match the filters.
      content:
        application/json:
          schema:
            "$ref": "./V2DomainsListDomainsResponseBody.yaml"
          examples:
            domainList:
              summary: Domain list
              description: Two verified domains. The routing record verified the subdomain. The TXT record verified the apex domain, because Unkey cannot read its alias back.
              value:
                meta:
                  requestId: req_1234abcd
                data:
                  - id: dom_1234abcd
                    domain: api.acme.com
                    projectId: proj_1234abcd
                    appId: app_1234abcd
                    environmentId: env_1234abcd
                    status: verified
                    dnsRecords:
                      - type: CNAME
                        name: api.acme.com
                        value: a1b2c3d4e5f6g7h8.cname.unkey.com
                        ttl: 60
                        verified: true
                        note: Create as DNS-only if your provider offers the choice.
                      - type: TXT
                        name: _unkey.api.acme.com
                        value: unkey-domain-verify=3ZQ8xK1mP7vT5nR2wY6bJ4hL
                        ttl: 60
                        verified: false
                        note: Proves ownership. Create it alongside the routing record.
                    createdAt: 1704067200000
                    updatedAt: 1704067260000
                  - id: dom_5678efgh
                    domain: acme.com
                    projectId: proj_1234abcd
                    appId: app_1234abcd
                    environmentId: env_1234abcd
                    status: verified
                    dnsRecords:
                      - type: ALIAS
                        name: acme.com
                        value: i9j8k7l6m5n4o3p2.cname.unkey.com
                        ttl: 60
                        verified: false
                        note: Apex domains cannot hold a CNAME. Use ALIAS, ANAME, or a flattened CNAME depending on your provider.
                      - type: TXT
                        name: _unkey.acme.com
                        value: unkey-domain-verify=7Kp2Rq9wZx4mB6nT1yL8vJ3h
                        ttl: 60
                        verified: true
                        note: Proves ownership. An apex domain cannot be verified through its routing record, so this is the only proof available.
                    createdAt: 1704067200000
                    updatedAt: 1704067260000
                pagination:
                  hasMore: false
            empty:
              summary: No domains attached
              description: A workspace or filtered resource with no custom domains returns an empty array, not a 404.
              value:
                meta:
                  requestId: req_1234abcd
                data: []
                pagination:
                  cursor: null
                  hasMore: false
    "400":
      description: Bad request
      content:
        application/json:
          schema:
            "$ref": "../../../../error/BadRequestErrorResponse.yaml"
          examples:
            searchTooLong:
              summary: Search string above the maximum
              value:
                meta:
                  requestId: req_1234abcd
                error:
                  title: Bad Request
                  detail: The search string must be at most 256 characters.
                  status: 400
                  type: validation-error
                  errors:
                    - location: body.search
                      message: must be at most 256 characters
                      fix: Shorten the search string, or omit it to return domains without a search filter
    "401":
      description: Unauthorized
      content:
        application/json:
          schema:
            "$ref": "../../../../error/UnauthorizedErrorResponse.yaml"
          examples:
            invalidRootKey:
              summary: Root key not found
              value:
                meta:
                  requestId: req_1234abcd
                error:
                  title: Unauthorized
                  detail: The provided root key is invalid.
                  status: 401
                  type: unauthorized
    "403":
      description: |
        Forbidden - The root key or its workspace is disabled. An unfiltered workspace list also
        returns 403 when the root key does not have the required permission. A filtered project,
        app, or environment list returns 404 for the same permission failure.
      content:
        application/json:
          schema:
            "$ref": "../../../../error/ForbiddenErrorResponse.yaml"
          examples:
            keyDisabled:
              summary: Root key disabled
              value:
                meta:
                  requestId: req_1234abcd
                error:
                  title: Forbidden
                  detail: The provided root key is disabled.
                  status: 403
                  type: forbidden
    "404":
      description: |
        Not Found - A requested project, app, or environment does not exist in your workspace, or
        your root key cannot read its domains.
      content:
        application/json:
          schema:
            "$ref": "../../../../error/NotFoundErrorResponse.yaml"
          examples:
            environmentNotFound:
              summary: Environment not found
              value:
                meta:
                  requestId: req_1234abcd
                error:
                  title: Not Found
                  detail: The requested environment does not exist.
                  status: 404
                  type: not-found
    "429":
      description: Too Many Requests
      content:
        application/json:
          schema:
            "$ref": "../../../../error/TooManyRequestsErrorResponse.yaml"
          examples:
            rateLimited:
              summary: Workspace rate limit exceeded
              value:
                meta:
                  requestId: req_1234abcd
                error:
                  title: Too Many Requests
                  detail: You have exceeded the rate limit for this operation. Retry after the window resets.
                  status: 429
                  type: rate-limited
    "500":
      description: Internal server error
      content:
        application/json:
          schema:
            "$ref": "../../../../error/InternalServerErrorResponse.yaml"
          examples:
            internalError:
              summary: Unexpected server error
              value:
                meta:
                  requestId: req_1234abcd
                error:
                  title: Internal Server Error
                  detail: An unexpected error occurred. Contact support@unkey.com with the requestId.
                  status: 500
                  type: internal-server-error
