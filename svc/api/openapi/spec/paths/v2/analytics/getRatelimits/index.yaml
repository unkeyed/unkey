post:
  tags:
    - analytics
  security:
    - bearer: []
  x-speakeasy-name-override: getRatelimits
  operationId: analytics.getRatelimits
  summary: Query rate limit data
  description: |
    Queries may reference only the five public rate limit analytics aliases: `ratelimits_v1`, `ratelimits_per_minute_v1`, `ratelimits_per_hour_v1`, `ratelimits_per_day_v1`, or `ratelimits_per_month_v1`. CTEs, subqueries, UNION, and EXCEPT are supported.
    Every query must include a positive literal `[alias.]namespace_id = '...'` or `[alias.]namespace_id IN (...)` filter containing 1 to 10 unique namespace IDs.
    Workspace retention and query limits apply.
  requestBody:
    required: true
    content:
      application/json:
        schema:
          "$ref": "./V2AnalyticsGetRatelimitsRequestBody.yaml"
  responses:
    "200":
      content:
        application/json:
          schema:
            "$ref": "./V2AnalyticsGetRatelimitsResponseBody.yaml"
      description: Query executed successfully
    "400":
      content:
        application/json:
          schema:
            $ref: "../../../../error/BadRequestErrorResponse.yaml"
      description: Invalid SQL query, blocked operation, or missing required authentication
    "401":
      content:
        application/json:
          schema:
            $ref: "../../../../error/UnauthorizedErrorResponse.yaml"
      description: Invalid authentication credentials
    "403":
      content:
        application/json:
          schema:
            $ref: "../../../../error/ForbiddenErrorResponse.yaml"
      description: Insufficient permissions
    "404":
      content:
        application/json:
          schema:
            $ref: "../../../../error/NotFoundErrorResponse.yaml"
      description: Referenced namespace not found
    "412":
      content:
        application/json:
          schema:
            $ref: "../../../../error/PreconditionFailedErrorResponse.yaml"
      description: Analytics is not configured for the workspace
    "422":
      content:
        application/json:
          schema:
            $ref: "../../../../error/UnprocessableEntityErrorResponse.yaml"
      description: Query exceeds workspace query limits
    "429":
      content:
        application/problem+json:
          schema:
            $ref: "../../../../error/TooManyRequestsErrorResponse.yaml"
      description: Query quota exceeded
    "500":
      content:
        application/json:
          schema:
            $ref: "../../../../error/InternalServerErrorResponse.yaml"
      description: Query execution failed
    "503":
      content:
        application/json:
          schema:
            $ref: "../../../../error/ServiceUnavailableErrorResponse.yaml"
      description: Connection to the database failed
