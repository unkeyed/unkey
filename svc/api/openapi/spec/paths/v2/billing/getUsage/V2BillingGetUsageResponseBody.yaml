type: object
required:
  - meta
  - data
properties:
  meta:
    $ref: "../../../../common/Meta.yaml"
  data:
    type: array
    description: One entry per end-user identity with billable usage in the period.
    items:
      "$ref": "./V2BillingGetUsageResponseData.yaml"
