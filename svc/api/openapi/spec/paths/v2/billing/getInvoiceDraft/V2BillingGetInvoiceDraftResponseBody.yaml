type: object
required:
  - meta
  - data
properties:
  meta:
    $ref: "../../../../common/Meta.yaml"
  data:
    type: array
    description: One draft entry per end-user identity with usage in the period.
    items:
      "$ref": "./V2BillingGetInvoiceDraftResponseData.yaml"
