type: object
description: |
  Lists all portal configurations in the authenticated workspace. Takes no
  parameters; the workspace is derived from the root key.
properties: {}
additionalProperties: false
examples:
  listAll:
    summary: List all configurations
    description: List every portal configuration in the workspace
    value: {}
