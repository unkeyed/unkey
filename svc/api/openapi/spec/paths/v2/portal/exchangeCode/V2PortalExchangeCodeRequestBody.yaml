type: object
required:
  - code
properties:
  code:
    type: string
    minLength: 1
    x-unkey-redact: true
    description: |
      The exchange code carried by the portal URL from `portal.createSession`.
      Must be valid, unexpired, and not previously redeemed.
    example: pst_abc123def456
additionalProperties: false
