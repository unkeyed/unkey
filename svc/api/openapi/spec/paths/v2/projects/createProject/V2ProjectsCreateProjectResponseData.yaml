type: object
required:
  - id
  - slug
properties:
  id:
    type: string
    minLength: 8
    maxLength: 255
    pattern: "^[a-zA-Z0-9_]+$"
    description: |
      The unique identifier of the newly created project, generated by Unkey.
      Always begins with 'proj_' followed by a unique alphanumeric sequence.
    example: proj_1234abcd
additionalProperties: false
