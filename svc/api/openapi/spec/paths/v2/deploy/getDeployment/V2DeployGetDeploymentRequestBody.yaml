type: object
required:
  - deploymentId
properties:
  deploymentId:
    type: string
    minLength: 1
    pattern: "^d_[a-zA-Z0-9]+$"
    description: Unique deployment identifier to retrieve
    example: "d_abc123xyz"
