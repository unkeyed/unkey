type: object
description: Prebuilt Docker image configuration
required:
  - image
properties:
  image:
    type: string
    minLength: 1
    description: Prebuilt Docker image reference
    example: "nginx:latest"
