type: object
additionalProperties: false
required: [name, mode]
properties:
  name: { type: string, minLength: 1, maxLength: 256 }
  mode:
    type: string
    enum: [set]
    x-enum-varnames: [LogdrainHeaderSet]
  value:
    type: string
    minLength: 1
    maxLength: 8192
    writeOnly: true
    x-unkey-redact: true
