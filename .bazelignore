web

# we need to figure out how to do the proto generation in bazel
proto
svc/ctrl/proto
svc/frontline/proto
svc/krane/proto
svc/vault/proto

# Claude Code agent worktrees are copies of the repo, not part of this build
.claude

# mise task scripts; filenames like `build` collide with Bazel BUILD on case-insensitive filesystems
.mise

# pricing is a standalone Go module with its own go.mod (it pulls in stripe-go,
# which we keep out of the root module); run it via `mise run pricing`.
tools/pricing
