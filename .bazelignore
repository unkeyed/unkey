web

# we need to figure out how to do the proto generation in bazel
proto
svc/ctrl/proto
svc/krane/proto
svc/sentinel/proto
svc/vault/proto

# Claude / git worktrees are full repo copies. Without this entry gazelle
# walks into them, sees duplicate importpaths, and produces confused
# BUILD.bazel diffs across the whole tree.
.claude/worktrees
