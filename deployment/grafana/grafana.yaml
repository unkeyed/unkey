apiVersion: 1

auth:
  basic:
    enabled: false
  anonymous:
    enabled: true
    org_name: Local
    org_role: Admin
datasources:
- name: Prometheus
  type: prometheus
  url: http://prometheus:9090
  isDefault: true
  access: proxy
  editable: true
