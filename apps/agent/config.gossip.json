{
  "$schema": "schema.json",
  "port": 8080,
  "nodeId": "${NODE_ID}",
  "region": "${FC_AWS_REGION}",

  "logging": {},

  "services": {
    "ratelimit": {
      "authToken": "secret"
    }
  },
  "cluster": {
    "serfAddr": "${HOSTNAME}",
    "rpcAddr": "${HOSTNAME}",
    "join": "${GOSSIP_JOIN}"
  }
}
