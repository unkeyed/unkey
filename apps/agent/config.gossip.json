{
  "$schema": "schema.json",
  "port": 8080,
  "nodeId": "${NODE_ID}",
  "region": "${FC_AWS_REGION}",

  "logging": {},

  "services": {
    "ratelimit": {
      "authToken": "secret"
    }
  },
  "cluster": {
    "serfAddr": "${HOSTNAME}:10000",
    "rpcAddr": "${HOSTNAME}:10001",
    "join": "${GOSSIP_JOIN}"
  }
}
