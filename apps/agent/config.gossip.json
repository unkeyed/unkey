{
  "$schema": "schema.json",
  "port": 8080,
  "nodeId": "${NODE_ID}",
  "region": "${FC_AWS_REGION}",

  "logging": {},

  "services": {
    "ratelimit": {
      "authToken": "secret"
    }
  },
  "cluster": {
    "redisUrl": "${REDIS_URL}",
    "rpcAddr": "${HOSTNAME}:8080"
  }
}
