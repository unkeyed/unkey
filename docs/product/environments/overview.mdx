---
title: Environments
description: An environment is an isolated deployment target that groups configuration, variables, and deployments for a specific stage of your workflow.
---

import DeployBeta from "/snippets/deploy-beta.mdx";

<DeployBeta />

## TODO

- What an environment is: an isolated deployment target (production, preview, or custom)
- How environments are created automatically from branch-to-environment mapping
- Environment-scoped configuration: variables, domains, Sentinel policies, replica counts
- The relationship between environments and deployments (many deployments per environment)
- Sticky environment domains that always point to the latest deployment in that environment
