---
title: "Configuration"
description: "Configuration reference for the logdrain service"
---

{/* Code generated by tools/configdocs. DO NOT EDIT. */}
{/* Descriptions come from the Go doc comments in svc/logdrain/config.go. Edit those and run `mise run generate`. */}

import ConfigToml from "/snippets/config-toml.mdx";

<ConfigToml />

The schema maps to [`svc/logdrain/config.go`](https://github.com/unkeyed/unkey/blob/main/svc/logdrain/config.go).

<ResponseField name="instance_id" type="string">
  InstanceID identifies this process in logs and traces. Leave it empty to generate a unique ID at startup.
</ResponseField>

<ResponseField name="region" type="string" required>
  Region labels telemetry and SQL comments. It does not affect routing: any replica can lease any drain. Must not be empty.
</ResponseField>

<ResponseField name="database" type="string" required>
  Database is the MySQL DSN. MySQL stores drain configuration, delivery state, and leases. Must not be empty.
</ResponseField>

<ResponseField name="clickhouse" type="object">
  ClickHouse hosts the exported streams and the delivery telemetry.
  <Expandable title="Fields">
    <ResponseField name="clickhouse.url" type="string" required>
      URL is the ClickHouse DSN. The service reads exported streams and writes delivery telemetry over this one connection. Must not be empty.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="vault" type="object">
  Vault decrypts destination credentials. URL and token are required, enforced by Validate because the shared struct leaves them optional.
  <Expandable title="Fields">
    <ResponseField name="vault.url" type="string">
      URL is the vault service endpoint.
    </ResponseField>
    <ResponseField name="vault.token" type="string">
      Token is the bearer token used to authenticate with the vault service.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="poll_interval" type="duration" default="60s">
  PollInterval controls how often each replica scans for due drains. It is also the pause between delivery cycles of a caught-up drain, so it bounds the steady-state delivery latency.
</ResponseField>

<ResponseField name="watermark_lag" type="duration" default="5m">
  WatermarkLag delays each exported timestamp window to protect against late ClickHouse inserts.
</ResponseField>

<ResponseField name="batch_size" type="int" default="1000">
  BatchSize caps the events read and shipped per delivery attempt. Minimum: 1.
</ResponseField>

<ResponseField name="pause_threshold" type="int" default="50">
  PauseThreshold is the number of consecutive failures that moves a drain to paused_by_failure. Minimum: 1.
</ResponseField>

<ResponseField name="max_concurrent_drains" type="int" default="4">
  MaxConcurrentDrains caps how many drains one replica processes in parallel within a poll cycle. Minimum: 1.
</ResponseField>

<ResponseField name="observability" type="object">
  Observability configures logging, tracing, and the metrics listener. The metrics listener is the only HTTP server this service has; it serves /metrics and the /health probe endpoints on the same port.
  <Expandable title="Fields">
    <ResponseField name="observability.tracing" type="object">
      TracingConfig controls OpenTelemetry tracing and metrics export. SampleRate determines what fraction of traces are exported; the rest are dropped to reduce storage costs and processing overhead.
      <Expandable title="Fields">
        <ResponseField name="observability.tracing.sample_rate" type="float" default="0.25">
          SampleRate is the probability (0.0–1.0) that a trace is sampled. Minimum: 0. Maximum: 1.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="observability.logging" type="object">
      LoggingConfig controls log sampling. Events faster than SlowThreshold are emitted with probability SampleRate; events at or above the threshold are always emitted.
      <Expandable title="Fields">
        <ResponseField name="observability.logging.sample_rate" type="float" default="1.0">
          SampleRate is the probability (0.0–1.0) of emitting a fast log event. Set to 1.0 to log everything. Minimum: 0. Maximum: 1.
        </ResponseField>
        <ResponseField name="observability.logging.slow_threshold" type="duration" default="1s">
          SlowThreshold is the duration above which a request is always logged regardless of SampleRate.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="observability.metrics" type="object">
      MetricsConfig controls Prometheus metrics exposition.
      <Expandable title="Fields">
        <ResponseField name="observability.metrics.prometheus_port" type="int">
          PrometheusPort is the TCP port where Prometheus-compatible metrics are served. Set to 0 to disable metrics exposure.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="insecure_allow_private_endpoints" type="bool">
  InsecureAllowPrivateEndpoints disables the HTTP sink SSRF guard so drains can target loopback or private addresses. Development only.
</ResponseField>

